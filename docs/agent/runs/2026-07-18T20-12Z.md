# Run journal — 2026-07-18T20:12Z

**Stage:** 3 (plan) — productive quiet run. Resolved contradictory Stage-3 plans on **#177**
and caught a latent bug the winning plan would have shipped. No PR opened (guardrail active).

## Orientation (live GitHub + git @ `origin/main` 0cfdfd2)
- **Open `agent/*` PRs: 3** → #175 (`feat(web)` add catalog card to wallet, closes #174;
  `mergeable_state: clean`, 0 reviews — the one real feature in flight), #176 (journal, docs),
  #178 (journal + PRD re-sync, docs). **≥3-PR guardrail ACTIVE → no 4th PR this run.**
- **Open `agent` issues: 2** → #177 (rec-engine slice 4, fully planned — see below), #174
  (closed-by the open #175). Backlog otherwise drained.
- No CI (`.github/workflows/` absent) → nothing to iterate in Stage 5. No review comments on any PR.

## What this run did (Stage 3 — resolved a real plan conflict, not filler)
#177 had accumulated **two Stage-3 plans that contradict each other** on the core integration seam:
- Comment 1 (option A): change `analyze_portfolio` to return a dict, update `card_insight_engine.py`.
- Comment 2 (option B): keep the `List[dict]` contract, merge the new field at the snapshot layer.

Read the portfolio path end-to-end against `main` to adjudicate, and found **neither plan is fully
correct**:
- `analyze_portfolio` really returns `List[dict]` (`card_recommendation.py:322`,`:439`) and
  `card_insight_engine.py:107` consumes it as a list (`for analysis in portfolio`). → Option A's
  dict-return needlessly breaks that caller + every list-indexing test. **Option B's List contract wins.**
- **But** `recommendation_snapshot.py` has **two** return sites. Comment 2 caches the combined dict
  `{"cards":[...],"category_assignments":[...]}` but only rewrites the **cold path** (`:110–133`); the
  **cache-hit path** (`:93–99`, the common case) still wraps the stored blob under `result_key="cards"`
  → nested `{"cards":{"cards":[...],...}}`, breaking the `/portfolio` consumer that reads `.cards`.
  This is exactly the cache trap comment 1 flagged — comment 2 reintroduced it by touching one path.

Posted a single **plan-resolution comment** on #177
(issuecomment-5012756290): keep `analyze_portfolio` as a `List`, add a pure
`best_card_per_category` helper + shared `_category_rate`, and rewrite **both** snapshot paths for the
portfolio branch (spread the combined payload in cold AND cache-hit; keep `next_card` on its existing
`result_key` shape). Added a required **cache-hit regression test** (prime snapshot → re-read with
unchanged `inputs_hash` → assert flat `cards` list + `category_assignments`, no double-nesting) — the
regression both prior plans would have missed. The next PR slot can now go straight to a single clean
backend PR without re-litigating the seam.

## Why no code / PR
Guardrail active (3 open PRs). #177 implementation is the next Stage-4 step once a slot frees; this run
de-risked it. Consistent with the run contract: a quiet run that advances planning is a good run — no
filler PR. Journal appended to the existing #178 branch (zero-conflict, docs-only) rather than opening a 4th PR.

## QUESTIONS FOR HUMAN (standing — all owner-gated, unchanged)
1. **Merge #175** — verified clean-mergeable; completes browse → detail → add-to-wallet.
2. **#171 / rates** — bless the 14 verified category rates; keep vs. down-rate the optimistic
   narrow-earn travel rates (Amex Platinum travel:5 / Gold travel:3).
3. **#72** — enable GitHub Actions so CI can gate PRs.
4. **#177 defaults** (non-blocking) — assign only spent-in categories (default) vs. all 10; tie-break
   `(-rate, annual_fee, name)`.

# Run journal — 2026-07-10T02-10Z

## Orientation (state at start)
- `origin/main` @ `8955820`. **Open agent PRs: 0** (queue clear). **Open agent issues: 1** — #38 (category-rate data source, BLOCKED on an owner A/B/C decision; unanswered across several runs — the only comment is our own Stage-2 research).
- Seed pipeline otherwise advanced: Plaid Phase 1 (#5–#8) and Phase 2 (#11, #23) closed; rec-engine slices — flat earn #35, fee-waiver #41, alt-exclusions #44, cache dataset-key #47 — all merged. #38 (slice 3, category-aware earn) is the sole remaining rec-engine work and gates slices 3–5 + the dining-first premise.
- No CI on the repo; sandbox can't `pip install -e ".[dev]"` (plaid-python wheel build fails) → DB-backed pytest not runnable here. Verification is owner-local.

## Decision
- #38 is blocked on the owner — must NOT start category-aware code (all of slices 3–5 gate on the unmade data-source decision). PR queue is clear and no new unblocked correctness gap surfaced (the last two runs already hunted these and shipped #47). Per the guardrails, a quiet planning run is the right move — **never open a filler feature PR**.
- Advanced **Stage 3 (Plan)** for #38 on the recommended path (A): posted a ready-to-execute implementation plan as an issue comment, explicitly labeled contingent so it does **not** presume the pick. Goal: zero design latency the moment the owner chooses (A); discarded harmlessly if they choose (B)/(C).

## What I advanced — Stage 3 (plan pre-staged on #38)
Re-verified the two seams the plan depends on, then wrote the plan:
- `spending_profile.py:84` — `category_breakdown` = category → **avg monthly dollars** (per-category annualized spend already available).
- `card_recommendation.py:84` — `_ongoing_value` is the single earn seam both `recommend_next_card` and `analyze_portfolio` route through; extending only this helper keeps the two modes from drifting.
- Plan covers: new `app/data/card_category_rates.json` (cardId → {category: percent-equivalent rate} + `_meta` provenance), the additive `_ongoing_value` extension (absent card/category ⇒ exactly today's flat number), tests (ranking flips with dining share; golden-number regression proving additivity; points/`points_value_cents` caveat), verification, and folded-in risks. Comment: #38#issuecomment-4931333111.
- Wrote no code — the decision is still the owner's.

## State after run
- Open agent PRs: **0** (this journal PR only). Open agent issues: **#38** (blocked on owner). Well under the 3-PR guardrail.

## QUESTIONS FOR HUMAN (carried — blocking; recorded on their issues)
1. **[BLOCKING rec-engine slices 3–5 + the dining-first premise]** #38 — category-rate data source. Pick **(A)** in-repo curated category-rate table for the top ~20–30 cards + flat fallback [recommended; plan now pre-staged and ready], **(B)** adopt an alternative structured dataset, or **(C)** defer category-awareness and ship flat-cashback recs as v1. **This is the sole blocker on all remaining core rec-engine work and the PR queue is empty — the agent has nothing else in-scope to build until you decide.**
2. **[carried, several runs]** #22 (manual-import epic) is CLOSED but only slice 1 shipped — reopen to track slices 2–4, or intentionally dropped?

## Next run (suggested)
- If #38 still unanswered: hold — do not start category code. With the plan pre-staged and no unblocked gap, keep to doc/research/planning (e.g. Phase-2 spending-intelligence follow-up research) rather than churning a filler PR.
- If owner picks (A): execute the pre-staged plan on `agent/38-category-earn` → PR. If (B)/(C): re-plan to the chosen path.

## Links
- Plan comment: **#38#issuecomment-4931333111** · Blocked/awaiting owner: **#38**, **#22**.

# Agent run — 2026-08-02 20:20 UTC

## Stage advanced
**Stage 4 (Implement)** for **issue #185** — rec-engine slice 5: multi-card
combination (`optimal_card_combination`). The final rec-engine slice named in
`docs/prd/recommendation-engine.md` and the headline capability of
`docs/prd/PRODUCT.md` Decision #1 ("recommend across BOTH existing + new cards").

## Why this item
- Orientation: 3 open PRs — two `agent/*` (#240 enrichment provenance, #239 web
  lint) both `mergeable_state: clean`, no CI configured, no review comments →
  nothing to iterate (Stage 5 idle); #238 (`feat/auto-extract`) is owner-authored,
  not an agent branch.
- Open agent issues: #229 (→ PR #239), #208 (→ PR #240 slice 2a; slice 2b gated on
  owner provider/privacy decision), #211 (blocked on owner — Plaid Liabilities),
  #210 (blocked on owner — build-our-own-MCP), #185.
- **#185 was the only unblocked engineering item.** Its sole hard dependency
  (#181 slice 4) is merged — verified `_category_rate` / `best_card_per_category`
  present on `main`. Its Stage-3 plan is complete and its questions are
  explicitly non-blocking (defaults proposed). Prior runs deferred it only for the
  ≥3-open-`agent/*`-PR guardrail, which has drained to 2. Guardrail respected
  (opening this makes 3, not 4+).

## What shipped (PR — branch `agent/185-optimal-card-combination`)
- `app/services/card_recommendation.py`: pure `_route_wallet` (shared routing
  primitive reusing slice-4's `_category_rate` + tie-break) and public
  `optimal_card_combination(profile, user_cards, available_cards, *,
  max_new_cards=1, points_value_cents=1.0)` — greedy, deterministic, bounded;
  marginal Δ = extra earn + achievable bonus + first-year credits − first-year fee.
- `app/schemas/recommendation.py`: additive `RoutedCategory`, `RecommendedNewCard`,
  `CombinationResponse` (no existing keys touched).
- `app/services/recommendation_snapshot.py` + `app/api/recommendations.py`: new
  `combination` rec_type through the same `_shape_payload` cache discipline;
  `GET /recommendations/combination`; added to `invalidate` + `/refresh` warm set.
- `tests/test_card_recommendation_service.py`: `TestOptimalCardCombination` (11
  hermetic cases — baseline routing, recommend-nothing, add-one-wins, fee-drop,
  multi-tier bonus, waived fee, tie-break, empty profile, empty-wallet-starter,
  owned/discontinued exclusion).
- `CHANGELOG.md` `[Unreleased]`.

## Verification
- `ruff check app/ tests/` → clean.
- Full suite via `uv` venv + `./scripts/setup-test-db.sh` (throwaway Postgres, per
  `CLAUDE.md`): **322 passed**.

## Questions for human
Carried on the PR (all non-blocking, defaults applied): include `_credit_value` in
the marginal Δ (yes, matches `recommend_next_card`); `max_new_cards` default = 1;
spent-in vs. all-10 categories inherited from #181 Q1. No product decision guessed.

## Next
Frontend rendering of the combination is a separate fast-follow slice (mirrors the
slice-4 → #183 sequencing). Owner decisions still pending unblock #208 slice 2b,
#211, #210.

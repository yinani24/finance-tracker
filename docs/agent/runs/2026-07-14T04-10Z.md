# Run journal — 2026-07-14T04:10Z

**Type:** substantive — Stage 4 feature PR. Closed the one genuine gap on owner-filed top-priority issue **#149** (CSV import). First feature PR after ~18 owner-gated runs.

## What I advanced
Issue #149 ("Statement upload: ingest transactions from CSV") was found last run to be **already shipped end-to-end** (#22 + #114/#115) with exactly **one unmet acceptance criterion**: criterion 5 — *"Malformed rows are reported in `errors[]`, not silently dropped."* Today malformed rows were only **counted** (`skipped`), with no per-row detail.

This is an owner-**specified** requirement (the owner literally wrote `errors[]` in the issue), not a product decision — the shape `[{row, reason}]` is the single obvious engineering answer. The prior run deferred it as an "unconfirmed API decision"; on reflection that was over-cautious for a written acceptance criterion. Shipped it as a small, additive, back-compat slice and flagged the shape assumption for the owner (non-blocking).

## Change (branch `agent/149-import-row-errors`, PR closes #149)
- **`app/services/statement_import.py`**: new `RowError{row, reason}` dataclass; `parse_csv` now returns `(rows, errors)` instead of `(rows, skipped_count)`, capturing the 1-based file line number and the `ValueError` reason (`unrecognized date format…`, empty-amount, etc.) for each bad row. `ImportResult` gains `errors: list[RowError]` (`len(errors) == skipped`, count retained).
- **`app/schemas/import_record.py`**: new `RowError` pydantic model; `ImportSummary.errors: list[RowError] = []` (default empty → back-compat).
- **`app/api/imports.py`**: maps `result.errors` into the response.
- **`apps/web/src/lib/types.ts`**: `ImportRowError` + `ImportSummary.errors`.
- **`apps/web/src/components/statement-import.tsx`**: collapsible "Show N unparseable rows" `<details>` listing each `Row N: reason` under the success summary.
- **Tests**: updated 2 parser unit tests (`errors` list + row numbers `[3,4]` + reason text) and the `test_skipped_rows_counted` endpoint test (asserts `errors[0].row == 3` + reason). CHANGELOG updated.

## Verification (full, in-sandbox)
- Provisioned throwaway Postgres via `scripts/setup-test-db.sh` (uv venv, `uv pip install -e .[dev]`).
- **`pytest` → 235 passed** (13 imports tests, incl. new `errors[]` assertions).
- **`ruff check app/` → clean.**
- **`npx tsc --noEmit` → clean**; **eslint** on changed web files → clean.

## Notification
Sent — first shippable PR after a long gated stretch; the owner will want to review/merge #149 (and it closes the issue they filed as top priority).

## Live state (re-verified)
- Open agent PRs after this: **2** (#128 Explore-cards UI, this #149 errors PR) — under the 3-PR guardrail.
- Open agent issues: #72 (CI, gated on owner Q1 metered-Actions consent), #38 (closed; owner A/C confirm open).

## QUESTIONS FOR HUMAN (standing — any ONE unblocks the pipeline)
1. **Review/merge this PR (#149)** — closes the CSV-import issue you filed; also confirm the `errors[]` shape `{row, reason}` is what you wanted (trivial to adjust).
2. **Review/merge PR #128** — ready, keys-free Explore-cards browse UI.
3. **#38** — reply A (in-repo `card_category_rates.json`) or C (defer; flat-cashback v1) to unblock rec-engine slices 3–5.
4. **#72 Q1** — enable metered GitHub Actions so CI can gate PRs (pytest + `ruff check .` both green today)?

_Generated by Claude Code._

# Run journal — 2026-07-09T19-11Z

## Orientation (state at start)
- **Open PRs: 2** — #37 (product: ongoing-rewards slice, `Closes #35`) and #39 (docs: category-rate research for #38 + a journal). Under the 3-PR guardrail.
- **Open `agent` issues: 2** — #35 (fully specced, no blocking questions; addressed by #37) and #38 (blocked on a human data-source decision).
- No CI is configured on the repo (`get_status` → `total_count: 0`), so Stage 5 = own-review + merge, per prior runs' "review & land" pattern.
- Decision: prefer finishing in-flight work over starting new. Both open PRs were ready → land them, clear the queue.

## What I advanced — Stage 5 (review & land)

### 1. Merged #37 (squash) → `main` — **closes #35**
Reviewed the diff for correctness before merging:
- `_ongoing_value(cashback_pct, avg_monthly_spend) = avg_monthly_spend × 12 × cashback_pct / 100` is **arithmetically identical** to the inlined earn calc it replaces in `analyze_portfolio` (main calc + alternatives loop) → no behavior change there, genuine de-duplication.
- `recommend_next_card` score changes from `bonus − fee + credits` to `bonus + ongoing − fee + credits`, closing the half-of-objective gap called out in the PRD (Decision #1). Response is additive (`ongoing_value` key); achievability/sort machinery untouched.
- Tests thorough: ongoing math, zero-cashback, bonus+ongoing, achievability-still-skips, explanation surfaces all four components, and the A-vs-B ranking regression ($870 vs $515, A first). `TestCrossTypeRanking`'s two hard-coded scores updated to reflect the new (correct) term; cashback still ranks first, so intent preserved. CHANGELOG under `[Unreleased]`.
- `mergeable_state: clean`. **Rec-engine slice 2 (flat ongoing earn) is now on `main`.**

### 2. Merged #39 (squash) → `main`
Docs-only (two new files: `docs/agent/research/38.md`, a prior run journal). No overlap with #37 → no conflict. Lands the grounded category-rate research permanently and carries its QUESTIONS FOR HUMAN into the repo.

### Result
- **PR queue now clear (0 open `agent/*` PRs).**
- Open `agent` issues: **#38** only (blocked on human — see below). #35 auto-closed by the #37 merge.

## QUESTIONS FOR HUMAN (unchanged — still blocking / carried)
These are recorded on issue #38 and PR #39; surfacing here so they aren't lost:
1. **[BLOCKING rec-engine slices 3–5 + the dining-first premise] Category-rate data source.** The card dataset (`credit-card-bonuses-api`, 179 cards) has **no per-category reward rates** — only a flat `universalCashbackPercent` — and upstream is a read-only mirror that won't accept PRs, so "enrich the sibling API" is not available. Pick a path (detail in #38):
   - **(A) — recommended** — small in-repo curated category-rate table for the top ~20–30 cards, flat-`universalCashbackPercent` fallback for the long tail. Smallest slice that makes "recommend based on how much you dine out" real.
   - **(B)** adopt an alternative structured dataset (licensing/freshness to vet).
   - **(C)** defer category-awareness; ship the now-merged flat-cashback recs (#37) as v1.
2. **[carried, several runs] #22 (manual-import epic) is CLOSED but only slice 1 shipped** — slices 2–4 (multi-row/statement CSVs, dedupe-on-import, mapping UI) are real remaining work. Reopen to track, or intentionally dropped?

## Next run (suggested)
- If #38 is still unanswered: don't start new rec-engine code (slices 3–5 are all gated on it). Advance Plaid Phase-1 issues (#6 error handling → #7 webhook → #8 webhook JWT) or Phase-2 spending-intelligence instead, or do research/planning on those.
- If the owner answers #38 → implement the chosen data path as the next slice.

## Links
- Merged this run: **#37** (closes **#35**), **#39**.
- Open/blocked: **#38** (category-rate data source).

# Run 2026-07-09T08-16Z

## Stage advanced
**Stage 4 — Implement** issue #24 (recommendation engine: value sign-up bonuses in
dollars / cents-per-point). Opened PR to `main` from `agent/24-bonus-value-usd`.

## Context / orientation
- Open agent issues: #26 (alembic chain — PR #27), #24 (this), #23 (freq metrics —
  PR #25), #22 (manual import).
- Open agent PRs before this run: **2** (#27, #25) → under the 3-PR guardrail, so
  opening one more is allowed.
- Stage 5 check: PRs #27 and #25 have **no CI configured** (repo has no
  `.github/workflows/`) and **no reviews/comments** — nothing actionable; both are
  waiting on owner review/merge. Nothing for me to iterate on there.
- Picked #24: highest-priority ready item (Phase 4, the single most fundamental
  correctness gap), fully planned in its Stage-3 comment, decision-independent
  (proceed at 1.0¢ default), **no migration**, and **not blocked** by the alembic
  chain issue.

## What changed (per the issue's Stage-3 plan)
- `app/config.py` — new `points_value_cents: float = 1.0` (`FT_POINTS_VALUE_CENTS`).
- `app/services/card_recommendation.py` — `_bonus_value_usd(offer, rate)` values USD
  entries at face value and points/miles at the blended rate; `_bonus_points` sums raw
  points for display; `_best_offer` now ranks by the same USD valuation;
  `recommend_next_card` takes `points_value_cents` (default 1.0), so `score` /
  `bonus_value` / explanation are all dollar-denominated.
- Callers inject `settings.points_value_cents`: `recommendation_snapshot.py`,
  `card_insight_engine.py`. Also fixed a stale `"{n} pts"` label in the insight
  data-point (now `${n}`); the pre-existing `impact_one_time_cents = bonus_val*100` is
  now correct (dollars→cents).
- `.env.example`, `CHANGELOG.md` (`[Unreleased] → Changed`).
- Tests: USD passthrough, points→$ at default + configurable rate, mixed offer,
  `_bonus_points`, and the **cross-type ranking regression** (a 90k-pt/$600-fee card vs
  a $500/$0-fee cashback card — old code ranked points first on magnitude, new code
  ranks cashback first on real dollar value). Updated the existing Platinum assertion
  to dollar reasoning (still #1; `bonus_value == 1500.0`).

## Verification
- pytest not installed in sandbox → validated pure-function logic via standalone
  scripts: all valuation cases, the cross-type reversal, explanation string, and the
  Platinum SAMPLE_CARDS case (bonus_value 1500, score 1145) pass.
- `ruff check` clean on all lines I added/changed (remaining E501s are pre-existing on
  `main` in unchanged lines). All changed files AST-parse.
- Full `pytest` + live-DB paths are owner-local (see PR VERIFY LOCALLY).

## Human decisions flagged
Carried into the PR under QUESTIONS FOR HUMAN: (1) confirm 1.0¢/point blended rate;
(2) per-program valuation later (needs the sibling dataset enriched with a program id).
Neither blocks this slice.

# Run journal — 2026-07-10T08-13Z

## Orientation (state at start)
- `origin/main` @ `999112c` (fetched; no concurrent run).
- **Open agent PRs: 0.** **Open agent issues: 1** — #38 (category-rate data source),
  BLOCKED on the owner's (A)/(B)/(C) pick. #22 (manual-import epic) CLOSED, also
  awaiting an owner decision. Both carry unanswered QUESTIONS FOR HUMAN across many
  prior runs.
- Under the 3-PR guardrail. No CI on repo; sandbox can't build `plaid-python` → DB
  pytest is owner-local.

## What I advanced — **Stage 0 (PRD)**, and the reason it's not a quiet/no-op run
Prior runs correctly refused to open filler PRs or guess the blocked product decisions.
This run found a genuinely **unblocked** forward step they had missed: **Phase 2
(Spending intelligence) is an owner-confirmed roadmap phase with NO PRD**, while
Phases 1/4–5 all have one. Writing it is exactly the Stage-0 doc work the rules bless,
and — critically — it is **consumer-side, needing no card data**, so it is independent
of the #38/#22 blockers.

Added **`docs/prd/spending-intelligence.md`**, grounded in the actual code (not
invented):
- Documents the **shipped baseline**: ingest-time categorization
  (`enrichment/taxonomy.py`, 10-category set, applied on both Plaid and CSV paths) +
  `spending_profile.py`'s `avg_monthly_spend`, `category_breakdown` (monthly $/cat),
  `category_counts`, `top_merchants`, and `get_or_refresh` freshness.
- Names the **gap** vs `PRODUCT.md`'s literal Phase-2 promise: the *"how many times per
  month do I dine out"* metric is not surfaced — `category_counts` is a raw per-period
  total, not normalized to per-month, not exposed as a dining headline (FR3).
- Records two more candidate gaps (freshness edge when the lookback window slides; no
  recurring-vs-discretionary separation; no trend) with an **unblocked Stage-1
  decomposition** — FR3 frequency slice first, then the freshness-edge correctness
  fix, both filable/shippable now without any owner decision.

Also verified this run (fresh audit of previously-unaudited code — **no bug found**):
- `statement_import.py` (CSV ingest) — amount/date parsing, dedupe, enrichment hook all
  correct within the documented slice-1 "single-signed-amount CSV" scope.
- Sign/dedupe conventions are **consistent across sources**: Plaid stores `-amount` and
  hashes `amount` (outflow-positive); CSV mirrors it (`outflow_amount = -signed`);
  `spending_profile` aggregates with `abs()` + the `is_income` flag, so sign can't
  corrupt totals. `dedupe.py` contract matches both callers.
- `enrichment/taxonomy.py` — alias map + `other` fallback correct.

## Why this beats another quiet run
The unblocked *implementation* backlog was genuinely exhausted, but the unblocked
*documentation* backlog was not: a confirmed phase had no PRD. This PRD converts the
half-day stall into concrete, owner-independent next work (the FR3 dining-frequency
slice), so future runs have something to build even if #38/#22 stay unanswered.

## State after run
- Open agent PRs: 1 (this docs PR). Open agent issues: still #38 (blocked).
- No product decision guessed; the PRD's own decisions (frequency definition, trend,
  recurring separation) are recorded under QUESTIONS FOR HUMAN, not assumed.

## QUESTIONS FOR HUMAN (carried + new)
1. **[BLOCKING rec-engine slices 3–5]** **#38** — pick the category-rate data source:
   (A) in-repo curated table [recommended, fully planned], (B) third-party dataset,
   or (C) defer category-awareness. Unanswered across many runs; gates all rec-engine
   work. *(Note: this is a CARD-side block; the spending-intelligence PRD below is not
   affected by it.)*
2. **[unblocks a Plaid-free data path]** **#22** — manual-import epic is CLOSED with
   only slice 1 shipped. Reopen for slices 2–4 (column-mapping/preview → PDF/OFX →
   frontend), or done for now? If reopened: CSV-first vs PDF-first, local-disk vs
   object storage.
3. **[new, from the Phase-2 PRD — NON-blocking, has safe defaults]** In
   `docs/prd/spending-intelligence.md`: (a) dining "frequency" = per-transaction vs
   distinct-days? (per-transaction is the safe default to ship first); (b) trend/recency
   weighting wanted? (c) separate recurring charges from discretionary spend in Phase 2,
   or defer? These scope the unblocked Stage-1 slices but don't block filing FR3.

## Next run (suggested)
- If #38 or #22 lands → Stage 4 on the chosen path.
- **Else (both still blocked):** the newly-unblocked path — file the **FR3 per-month
  category-frequency** issue from this PRD (Stage 1), then implement it (Stage 4). It
  needs no owner decision (per-transaction default) and delivers the literal
  `PRODUCT.md` dining-frequency promise. This is real forward motion that does not
  depend on the two stuck decisions.

## Links
- New PRD: `docs/prd/spending-intelligence.md` · PR: this run's
  `agent/prd-spending-intelligence` (docs-only) · Still blocked on owner: #38, #22.

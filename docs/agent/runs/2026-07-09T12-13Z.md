# Run 2026-07-09T12-13Z

## Stage advanced
**Stage 5 — Iterate/land.** Merged the highest-priority in-flight PR after
reviewing its diff for correctness. Pipeline was at the 3-PR guardrail
(#25, #28, #29 open), so no new work was started — the run cleared in-flight
work instead.

## Orientation
- Open agent issues before run: #22 (manual import), #23 (freq metrics — PR #25),
  #24 (bonus-value USD — PR #28). #26 already closed by merged #27.
- Open agent PRs before run: **3** → #28 (bonus USD valuation), #25 (freq metrics),
  #29 (previous run journal). Guardrail hit → advance Stage 5 / docs only, no new PR.
- No CI configured in the repo (0 checks on every PR); mergeability is the only gate.
- All PR comments are agent-authored — **no genuine human review pending**; the open
  QUESTIONS FOR HUMAN on #28/#25 are non-blocking config-default confirmations.

## What I did
1. **Merged PR #28 (squash) → closes #24** — recommendation engine now values
   sign-up bonuses in **dollars**. Reviewed the full diff first:
   - `_bonus_value_usd(offer, rate)`: USD entries at face value, points/miles at
     `FT_POINTS_VALUE_CENTS` (default 1.0¢) — correct.
   - `score = bonus_val - annual_fee + credit_val` now all dollar-denominated
     (fees/credits were already $; only the bonus term was mis-united). This fixes
     the ~450× points-magnitude bias — the engine's single most fundamental
     correctness gap (Phase 4, PRODUCT.md Decision #1).
   - `_best_offer` ranks by the same USD valuation; both callers
     (`recommendation_snapshot.py`, `card_insight_engine.py`) inject
     `settings.points_value_cents`; stale insight `pts` label fixed.
   - Tests thorough: USD passthrough, points conversion at default + configurable
     rate, mixed offers, and the cross-type ranking regression. Clean/mergeable.
2. **Merged PR #29 (squash)** — previous run's journal (docs-only, no conflicts).
   Housekeeping to stop journal-PR pileup.
3. Wrote this journal.

## State after run
- Open agent PRs: **1** (#25). Open agent issues: #22, #23.
- `main` head advanced by the #28 fix.

## Follow-ups for next run (Stage 5 first)
- **PR #25 likely needs a rebase**: it adds a `[Unreleased] ### Added` CHANGELOG
  entry; #28 just added a `### Changed` entry under the same heading, so the two
  almost certainly conflict now (GitHub reported `mergeable_state: unknown` right
  after the merges). Rebase onto latest `main`, keep both CHANGELOG entries, then
  it's landable — its alembic blocker was already resolved by merged #27, and its
  migration chains cleanly off head `14fc7f6a99e3`.
- Then either land #25 or pick up #22 (manual CSV import, slice 1) / #23 remains
  open until #25 merges.

## QUESTIONS FOR HUMAN (carried, non-blocking)
From #28/#24 (now merged at the defaults — trivially reversible via env):
1. Confirm the **1.0¢/point** blended valuation (`FT_POINTS_VALUE_CENTS`), or set
   a different rate.
2. Per-program point valuation (Amex MR vs Delta vs Chase UR) needs the sibling
   `credit-card-bonuses-api` dataset enriched with a program identifier — worth a
   future cross-repo slice, or is a flat blended rate fine indefinitely?

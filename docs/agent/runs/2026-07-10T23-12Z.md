# Run journal — 2026-07-10T23-12Z

**Stage advanced:** Stage 1 (backlog) — filed **#72**, a real owner-independent
infra slice (CI test job), converting a recurring untracked journal footnote into
tracked backlog. No filler PR.

## Orientation
- `origin/main` @ `827998b` (#69) — fetched fresh.
- **#68 / PR #70 merged** (dependency caps + in-sandbox test unlock). #71 (verification
  journal) merged. **0 open PRs** — Stage 5 empty, nothing in-flight.
- Open `agent` issues at start: **1** — #38 (rec-engine slice 3, category-aware earn),
  fully gated on the owner's (A)/(B)/(C) card category-rate data-source pick. Its Stage-2
  research + Stage-3 plan are already staged on the issue; cannot reach Stage 4 without the
  decision.
- Spending-intelligence remaining slices (trend, recurring-charge) still gated on owner
  Open Q2/Q3. FR1–FR5 shipped.

## Why this move
The feature pipeline is fully owner-gated (#38 + spending Q2/Q3), so the highest-value
owner-independent step is Stage 0–3 doc/backlog work — not a filler PR. Every recent journal
(16-12Z, 18-23Z, 19-10Z) flagged **"add a CI workflow, now feasible headless"** as a genuine
owner-independent follow-up, but it was never captured in the authoritative backlog (GitHub
Issues). Filing it is exactly the blessed "quiet run that files issues is a good run" move —
and mirrors the #68 precedent (footnote → tracked issue → shipped).

Why it's feasible now: #70 proved the full 235-test suite runs headless and shipped the
`uv` + Postgres recipe. In GitHub Actions this is simpler still — a `postgres` service
container + `FT_TEST_DATABASE_URL` (env prefix `FT_`, `app/config.py:44`).

## What I did (all claims verified against `main` this run)
1. Confirmed **no CI exists** — `find .github -type f` returns nothing; no workflows dir.
2. Confirmed **no existing CI issue** (`search_issues` → 0 hits) to avoid a duplicate.
3. Verified the test/lint config: `pytest` `testpaths=["tests"]`; conftest → `test_database_url`
   (overridable via `FT_TEST_DATABASE_URL`); ruff `select=["E","F","I"]`, `line-length=100`.
4. Re-confirmed the pre-existing ruff debt (~65 issues, mostly `alembic/versions/`) that would
   make a repo-wide `ruff check .` gate red on `main` — so the proposal is **tests-only first**,
   with lint deferred behind a debt-clearing slice.
5. **Filed #72** — drafted an immediately-green `api-tests.yml` (Postgres service + `uv`),
   path-filtered to `apps/api/**`, with acceptance criteria and three QUESTIONS FOR HUMAN
   (enable Actions on a private/metered repo; whether/how to also gate lint; branch protection).
   Labeled `agent`.

## Verification
- No source/behavior change this run (issue + journal only). Every claim in #72 is grounded in
  `pyproject.toml`, `app/config.py`, and a `.github` filesystem check performed this run.
- Did not run `pytest` this run — no code changed; the 235-passed baseline was reproduced in the
  immediately-preceding runs (#70 author + #71 verification).

## Net pipeline state
- **Plaid Phase 1** — done. **Spending (Phase 2)** — FR1–FR5 shipped; trend/recurring gated on
  Open Q2/Q3. **Recommendation engine** — slices 1–2 shipped (#28, #37); slices 3–5 blocked on #38.
- **#68 (build reproducibility)** — shipped/merged (#70).
- **New: #72 (CI test job)** — owner-independent to implement once Actions-enable (Q1) is confirmed.

## QUESTIONS FOR HUMAN
No new *product* questions. Standing decisions (unchanged; not phone-notified to avoid hourly
noise — already recorded where you'll see them):
1. **#38** — pick the category-rate data source ((A) in-repo curated table recommended). Blocks
   rec-engine slices 3–5 and the dining-first core premise. Research + plan staged.
2. **Spending Open Q2/Q3** (`docs/prd/spending-intelligence.md`) — trend weighting + recurring-vs-
   discretionary modeling — before those slices can be filed.
3. **#72 (new)** — enable GitHub Actions on this private/metered repo? (+ whether to also gate
   lint, which needs the ~65 pre-existing `alembic/` ruff issues cleared first). Low-urgency.

## Links
- Filed: #72 · Standing gate: #38 · `origin/main` @ 827998b (#69)

# Run journal — 2026-07-10T18:23Z

## Stage advanced
**Stage 4 (Implement)** on **#68** — dependency caps + the in-sandbox test unlock.
Branch `agent/68-dep-caps` → PR (Closes #68).

## Context at start
- Open agent issues: **#68** (dependency pinning — owner-independent) and **#38** (rec-engine
  slice 3 — owner-gated on the category-rate data-source A/B/C decision).
- Open PRs: **#69** (prior run's journal, docs-only). 1 open PR → well under the 3-PR guardrail.
- Feature pipeline is owner-gated (#38, spending Open Q2/Q3), so the right move was the one
  actionable owner-independent item: **#68**.

## What I did
Picked #68 and — critically — discovered the "can't run pytest in-sandbox" limitation every
recent run has recorded is **solvable in-sandbox**, then shipped both the fix and the unlock:

1. **Root-caused the build failure.** `plaid-python` (all recent versions are sdist-only) fails
   under the sandbox's `pip 24.0`/setuptools (`AttributeError: install_layout`) — but **`uv`
   (0.8.17, present) builds it cleanly**. So the blocker was the *installer*, not the package.
2. **Brought up Postgres in-sandbox.** Postgres 16 binaries are installed; it refuses to run as
   root, so I run it as the `postgres` system user. Provisioned a `root` login role (the default
   `test_database_url` connects as the OS user) + the `finance_tracker_test` DB.
3. **Ran the FULL suite: `235 passed`** — first run to execute the real test suite in-sandbox
   rather than the `pytest --noconftest` pure-function subset.
4. **Fixed #68:** added compatible-release upper bounds to every dep in
   `apps/api/pyproject.toml`. Verified the caps by reinstalling a fresh venv under them →
   identical known-good resolution → **235 passed** again. `ruff` clean on changed files.
5. **Operationalized the unlock for future runs:** added `apps/api/scripts/setup-test-db.sh`
   (idempotent Postgres provisioner) and documented the `uv` + script + `pytest` recipe in
   `CLAUDE.md`. Updated `CHANGELOG.md`.

## Verification (in-sandbox, this run)
- `uv pip install -e ".[dev]"` under the new caps → known-good set (plaid-python 40.1.0,
  fastapi 0.139.0, sqlalchemy 2.0.51, pydantic 2.13.4, …), all inside the new ranges.
- `setup-test-db.sh` fresh + idempotent (re-run is a no-op).
- `pytest` → **235 passed** end-to-end via the script-provisioned DB.
- No new Python files, so no lint regression; `ruff check` on the changed paths is clean.

## Notes / observations
- **No CI exists** (`.github/` holds only the secret-scanning pre-commit hook — no workflows).
  So nothing gates lint/tests today. `ruff check .` at the newer pinned ruff surfaces **65
  pre-existing** issues (47×E501, 11×F401, 9×I001), almost all in `alembic/` migrations —
  pre-existing debt on `main`, out of scope for this PR. Candidate future issue: add a CI
  workflow (the recipe in this PR makes a real test job feasible) and/or clear the lint debt.
- A fully-pinned lockfile (strongest reproducibility, e.g. `uv pip compile`) was left as a
  possible follow-up — it's a workflow decision (how it's regenerated / CI-enforced) beyond the
  minimal cap fix #68 asked for first.

## Pipeline state
- Plaid Phase 1 — done. Spending FR1–FR5 — shipped. Rec-engine slices 1–2 — shipped (#28, #37).
- **#68** — this PR (caps + test unlock).
- **#38** — still blocked on owner (A)/(B)/(C) category-rate data-source pick.
- Spending trend/recurring slices — still blocked on owner Open Q2/Q3.

## QUESTIONS FOR HUMAN
No new product questions. Standing gates unchanged: **#38** (category-rate data source),
**spending Open Q2/Q3** (trend/recurring modeling). Optional infra follow-ups surfaced this
run: (a) add a CI workflow now that the full suite runs in-sandbox; (b) clear pre-existing
`ruff` debt in `alembic/`; (c) a fully-pinned lockfile on top of the caps.

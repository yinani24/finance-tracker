# Run journal — 2026-07-10T04-19Z

## Orientation (state at start)
- `origin/main` @ `93266e4`. **Open agent PRs: 0** (queue clear). **Open agent issues: 2** —
  - **#38** (category-rate data source) — BLOCKED on an owner A/B/C decision; plan already pre-staged (comment #4931333111). No agent action possible until the owner picks.
  - **#52** (split groceries from dining at ingest) — created 2026-07-10T03:14, dependency **#51 now merged** (`93266e4`), so it is **unblocked and ready**. Well-specified: names the files, the fix, and acceptance criteria.
- Pipeline otherwise: Plaid Phase 1 (#5–#8) + Phase 2 (#11, #23) closed; rec-engine slices #35/#41/#44/#47 merged; taxonomy-at-boundary #51 merged.

## Decision
- #38 stays blocked (owner) — did **not** touch category-aware code.
- Advanced **Stage 4 (Implement)** on **#52** — the single highest-priority *unblocked* item, a real correctness fix: grocery spend was folding into `dining`, inflating the dining-share signal the dining-first recommender ranks on and leaving the `groceries` bucket permanently empty.

## What I advanced — Stage 4 (implement #52) → PR
Branch `agent/52-split-groceries-dining` off latest `origin/main`.
- `app/services/plaid_service.py`: derive the raw category from Plaid's `personal_finance_category.**detailed**` **only when `primary == FOOD_AND_DRINK`** (the sole conflated primary); fall back to `primary` when `detailed` is absent. Non-food primaries keep using `primary` unchanged — a wholesale switch to `detailed` would send un-aliased labels like `TRANSPORTATION_GAS` to `other` (regression), so the split is scoped to the one primary that needs it.
- `app/services/enrichment/taxonomy.py`: added `_ALIASES` for the Plaid detailed food labels — `food and drink groceries` → `groceries`; `_restaurant`/`_coffee`/`_fast food`/`_beer wine and liquor`/`_vending machines`/`_other` → `dining`.
- Tests (`tests/test_enrichment.py`): `test_plaid_detailed_food_split` (mapper unit); sync-path tests `test_detailed_splits_groceries_from_dining_at_ingest`, `test_missing_detailed_falls_back_to_primary`, `test_non_food_primary_ignores_detailed`.
- `CHANGELOG.md` under `[Unreleased]`.

## Verification (in-sandbox)
- `ruff check` on all changed files — **clean**. `py_compile` — clean.
- `pytest tests/test_enrichment.py`: **11 hermetic tests pass** (incl. the new mapper test).
- The 6 `TestSyncEnrichmentHook` DB tests fail **only** on a pre-existing SQLite-vs-Postgres artifact — the app passes `occurred_on = txn_dict.get("date")` as a *string*, which Postgres coerces but SQLite's Date type rejects at INSERT (before enrichment even runs). This hits the **3 pre-existing** sync tests identically, so it is not a #52 regression; on Postgres these pass.
- To verify the real branch logic despite the sqlite date issue, ran `sync_transactions()` end-to-end against sqlite with a `date` object: `Whole Foods`(GROCERIES)→**groceries**, `Chipotle`(RESTAURANT)→**dining**, `Starbucks`(no detailed)→**dining** (fallback), `Shell`(TRANSPORTATION_GAS)→**transport** (non-food unaffected). **All four correct.**
- Note: plaid-python has no wheel and its sdist `bdist_wheel` fails on the Debian `install_layout` setuptools bug — extracted the pure-Python package onto `PYTHONPATH` to get the suite importable this run.

## State after run
- Open agent PRs: **1** (#52) + this journal. Open agent issues: **#38** (blocked on owner), **#52** (PR open, `Closes #52`). Under the 3-PR guardrail.

## QUESTIONS FOR HUMAN (carried — blocking; recorded on their issues)
1. **[BLOCKING rec-engine slices 3–5 + the dining-first premise]** #38 — category-rate data source. Pick **(A)** in-repo curated category-rate table (recommended; plan pre-staged), **(B)** adopt a structured dataset, or **(C)** defer category-awareness and ship flat-cashback recs as v1. Sole blocker on all remaining core rec-engine work.
2. **[carried]** #22 (manual-import epic) is CLOSED but only slice 1 shipped — reopen to track slices 2–4, or intentionally dropped?

## Next run (suggested)
- If #52 has review comments/CI: iterate (Stage 5). Once merged: with #38 still blocked and the queue clear, hold to doc/research/planning (e.g. Phase-2 spending-intelligence follow-up) — do not open a filler PR.

## Links
- PR: `agent/52-split-groceries-dining` (Closes #52). Blocked/awaiting owner: **#38**, **#22**.

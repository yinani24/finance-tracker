# Run journal — 2026-07-13T04:13Z

**Stage advanced:** Stage 1 (issues) — filed one new keys-free, product-loop slice (**#123**) after verifying every existing track is owner-gated.

## Orientation (read this run)
- **`origin/main`** at `83fae20` (03-11Z journal merged as #122).
- **Open `agent/*` PRs: 2** (both feature, complete + verified, owner-gated) — under the 3-PR guardrail:
  - **#119** — feat(web): edit & delete manually-added cards (`Closes #118`).
  - **#115** — feat(web): CSV statement import UI (`Closes #114`).
  - Re-verified this run: **both merge clean onto current `origin/main`** via `git merge-tree --write-tree` (the CHANGELOG add/add conflict was decoupled last run, commit `ae2b267`). #119 is 3 commits behind main, #115 is 5 — all intervening commits are docs-only journals, so no rebase needed (Rule 1).
  - **Nothing agent-actionable on either:** no CI in the repo (that's #72), zero review comments (#119's only comment is the agent's own CHANGELOG-decouple note), zero human reviews. Owner-gated by convention.
- **Open agent issues before this run: 3** — #114 (→#115), #118 (→#119), **#72** (CI, owner-gated on Q1).
- **PRDs** unchanged (`PRODUCT.md`, `plaid-integration.md`, `spending-intelligence.md`, `recommendation-engine.md`).

## Decision — why file a new issue rather than hold
Prior quiet runs (02-11Z, 03-11Z) correctly held off filing new work while the queue was "bottlenecked on owner review, not a shortage of scoped work." I re-examined that this run and found a **genuine, untracked, keys-free gap** worth documenting now so a future run has a scoped target the moment the review queue clears — a legitimate Stage-1 outcome ("a quiet run that files an issue is a good run"), and it adds **no** PR to the owner-review-bottlenecked queue.

Confirmed the core engine track is still blocked, not actionable:
- **#38** (category-aware earn, the dining-first premise) — owner **closed it as _completed_ with no A/B/C decision text**; the agent's "reply **A** or **C**" (2026-07-11) still has **no human reply**. Implementing either path would be guessing a product decision → **Rule 4 forbids it.** Still blocked.

## What I did — verified the gap from primary source, then filed #123
Cross-checked every mounted backend route against the web client (`api.ts`) + render sites:
- **Found:** the **card-catalog browse surface** is backend-complete and even half-wired on the client, but **renders nowhere.**
  - `GET /card-bonuses` (search: `q`/`issuer`/`network`/`is_business`/`max_annual_fee` + pagination), `GET /card-bonuses/issuers`, `GET /card-bonuses/{card_id}` — all mounted (`app/api/card_bonuses.py`, router `:22`).
  - `api.ts` already exports `searchCardBonuses(params)` and `types.ts` defines `CardBonus`/`CardBonusOffer`/`CardBonusSearchResult` — **yet `searchCardBonuses` is imported by no component** (grep → only its own def + type). `/card-bonuses/issuers` has **no client fn**. No "Explore/Browse cards" page or nav entry (`sidebar.tsx:33-40`).
- **Why it matters:** `PRODUCT.md`'s MVP loop step 3 is "match against the card dataset." Recommendations shows the single best pick; there's no surface to **browse** the 179-card public catalog (filter by issuer/network/fee, read sign-up offers). Keys-free — the data is the already-imported public sibling `credit-card-bonuses-api`; independent of #38 (that's category *rates*) and #72 (CI).
- **Ruled out as non-gaps:** the **chat** assistant (`/chat`) is fully wired (`components/chat.tsx`, dashboard); `me/preferences`, accounts/transactions/goals/cards CRUD, plaid, recommendations, insights all have render sites.
- Searched issues first — **no existing open/closed issue** tracks a card-catalog browse UI. Filed **#123** (labels `agent`, `prd:recommendation-engine`), tightly scoped: new `getCardBonusIssuers` client fn + one browse page wiring the existing `searchCardBonuses` + a nav entry; explicitly out-of-scope: "add to wallet" action and per-card detail page (fast-follows). Includes non-blocking QUESTIONS FOR HUMAN (route/label; whether to fold "add to wallet" in).

## State after
- **Open agent issues: 4** — #72 (CI, owner-gated), #114 (→#115), #118 (→#119), **#123 (new, ready to implement; unblocked, keys-free)**.
- **Open agent PRs: 3** — #119, #115 (feature, owner-gated) + this docs-only journal PR.
- No feature PR opened (queue already 2-deep unreviewed; #123 is the scoped target for when it clears).

## QUESTIONS FOR HUMAN (standing — no human reply since last run)
1. **#38 Q** — reply **A** (build in-repo curated category-rate table now; research + Stage-3 plan already staged in #38 comments) or **C** (flat-cashback is v1, mark slices 3–5 deferred). Gates the whole category-aware engine phase.
2. **#72 Q1** — enable metered GitHub Actions on this private repo for CI? Nothing gates tests on PRs until then.
3. **Spending-intelligence** — Q2 (trend weighting) / Q3 (recurring vs discretionary) in `docs/prd/spending-intelligence.md`.
4. **PRs #115 & #119** — complete, verified, keys-free, mutually conflict-free; ready to review + merge (each needs owner-local Supabase/Postgres for `VERIFY LOCALLY`).
5. **#123 (new)** — non-blocking: route/label for the browse page; whether to include an "add to wallet" action.

## Links
- New: **#123** (card-catalog browse UI).
- Open PRs: **#119** (`agent/118-card-edit-delete`), **#115** (`agent/114-csv-import-ui`).
- Owner-gated: **#38** (A/C), **#72** (CI Q1), spending-intelligence Q2/Q3.

# Run journal — 2026-07-09T14:24Z

## Orient
- `origin/main` @ `9b6b077` — healthy FastAPI + Next.js codebase (the ~10:10Z force-push flagged in **#30** was reverted; #30 is CLOSED, `apps/`, `docs/prd/`, `docs/agent/` all present).
- Open agent PRs at start: **#32** (previous run's journal, docs-only). Open agent issues: **#22** (manual CSV import) only.

## What I advanced
1. **Stage 5** — merged **#32** (docs-only run journal, `mergeable_state: clean`, no CI), clearing the PR queue to 0.
2. **Stage 4** — implemented **slice 1 of #22** (manual CSV bank-statement import, the Plaid-free ingest path) on branch `agent/22-csv-statement-import` → **PR (Closes #22)**.

## #22 slice 1 — what shipped
- `POST /imports` (multipart CSV + `account_id`) → stores the file, persists `Import`+`ImportFile`, parses single-signed-amount rows (tolerant date / `$` / comma / parenthesis handling) into **deduped** `Transaction`s (`source="import"`), runs them through the **same enrichment hook** as Plaid sync, returns an `ImportSummary` (added / duplicates / skipped / total).
- `GET /imports`, `GET /imports/{id}` → status + derived imported-transaction count.
- Idempotent re-upload (0 new on second upload); bad/empty/non-CSV file → `status="failed"` + **400**, no partial rows; unparseable rows skipped + counted.
- **Drift guard:** extracted the dedupe fingerprint (`app/services/dedupe.py`) and enrichment-apply (`app/services/enrichment/apply.py`) into shared modules; `plaid_service` now delegates to both (behavior unchanged — `_generate_dedupe_hash` / `_apply_enrichment` kept as aliases so existing tests still resolve).
- New `FT_IMPORT_STORAGE_DIR` (local dev storage). Added `python-multipart` dep.

## Verification (this run)
- Brought up Postgres 16 locally; **full suite green: 209 passed** (196 prior + 13 new `tests/test_imports.py`).
- Repaired 2 `test_enrichment.py` sync-hook tests whose `patch()` target moved with the refactor (now patch `app.services.enrichment.apply.get_provider`; assertions unchanged).
- `ruff` clean on all changed files. (No CI configured in this repo — no `.github/workflows`.)

## QUESTIONS FOR HUMAN (carried into the PR; non-blocking, proceeded on recommended assumptions)
1. **Amount sign convention** — slice 1 reads the CSV `amount` column as **negative = spend / positive = income** (matches the app-internal `Transaction.amount` sign). Some banks export the opposite; the column-mapping + preview slice (#22 slice 2) will let the user confirm/flip. Confirm this default.
2. **Format priority** — CSV-first (this slice). PDF/OFX deferred to a later slice — still the right order?
3. **File storage** — uploads on local disk (`FT_IMPORT_STORAGE_DIR`) for dev; real object storage (S3 / Supabase Storage) deferred. OK for now given these are sensitive documents?

## Next steps
- #22 slice 2 (column-mapping + preview, incl. sign confirmation), then slice 3 (PDF/OFX), slice 4 (frontend upload + manual account/txn forms).

# Agent run — 2026-08-02T01:11Z

**Stage:** Stage 4 (implement) — shipped a PR for **#201** (rec-engine correctness bug). Guardrail clear (1 open agent PR → below the ≥3 limit). First implementation run after the owner cleared the review queue.

## Orientation (state at run start)
- `origin/main` @ `bf5327c` (#216 "classify nearly everything") — **owner cleared the queue overnight**: main advanced #195 → #216 and open PRs dropped **7 → 2** since the 23:12Z run.
- **Open PRs: 2** — #217 `feat/metal-wallet` (owner branch, not `agent/*`) and #199 (`agent/*`, now stale/superseded, see below). Only **1 open `agent/*` PR** → 3-PR guardrail is clear, implementation permitted again.
- **Open issues: 11** (`agent`). Ready+planned correctness bugs still queued: **#201, #202, #203, #200**.
- **#181 merged** — `best_card_per_category` is on main (line ~475), so #201's "land after #181" precondition is satisfied.
- **CI:** still not enabled (#72) — no checks run on any PR.

## What I did (one step)
Implemented **#201** — `recommend_next_card` under-valued every cashback card's sign-up bonus by ~100× (a $250 cash bonus scored $2.50; 32 cards affected).
- Root cause: `_bonus_value_usd`/`_bonus_points` decided denomination from the **amount's own** `currency` field, but the upstream dataset denominates a bonus by the **card's** top-level `currency`. That field is present on only ~11 of 179 cards, so field-less dollar amounts fell to the points branch.
- Fix (`apps/api/app/services/card_recommendation.py`): shared `_amount_is_dollars(amount_currency, card_currency)` rule — explicit per-amount tag wins; field-less inherits the card currency; `card_currency=None` keeps legacy points fallback (non-regressive for bare unit calls). Threaded `card.get("currency")` into `_best_achievable_offer` and `recommend_next_card` (both bonus-value and explanation paths). Bonus-path sibling of the #192 credit fix.
- Tests: +9 in `test_card_recommendation_service.py` (unit denomination cases + two end-to-end `recommend_next_card` cases proving $250 bonus value and clean "Earn ~$250 in bonus value" copy).
- **Verification:** `ruff` clean; full suite via `uv` + `scripts/setup-test-db.sh` → **289 passed, 4 failed**. All 4 failures are **pre-existing on clean `main`** (confirmed by stash + re-run) — see below. My change adds zero regressions.
- Closes #201 via PR (branch `agent/201-cashback-bonus-currency`).

## Backlog hygiene this run
- **Closed #199 + #198 as superseded** — main already sources the Transactions category filter from `TAXONOMY_CATEGORIES` (`transactions/page.tsx:145`, `[null, ...TAXONOMY_CATEGORIES].map`), exactly #199's proposed fix. #199 was `mergeable_state: dirty` because it duplicated work already on trunk.
- **Filed a new issue** for the two test regressions #216 introduced (invisible without CI).

## QUESTIONS FOR HUMAN
1. **#216 silently broke 2 tests on `main`** (CI is off, so nothing caught it): `test_spending_profile_service.py::test_computes_categories_and_merchants` (a merchant now classifies `groceries`, not `travel`) and `test_recommendation_snapshot_service.py::test_dataset_change_recomputes` (`recommendations[0]` empty → IndexError). Filed as an issue. **Enabling GitHub Actions (#72) would have blocked this** — trunk is currently red.
2. **Next ready correctness bugs**, in priority order, each ~1 small hermetic-tested PR: **#202** ($3.3k offer-credits dropped, 14 cards) → **#203** (PDF-import spend↔income sign inversion — silent data corruption) → **#200** (Snooze 422 + effort badge). #202 has product calls noted in its issue.

_Generated by [Claude Code](https://claude.ai/code)_

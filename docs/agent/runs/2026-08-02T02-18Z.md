# Agent run — 2026-08-02T02:18Z

**Stage:** Stage 4 (implement) — fixed the red trunk (#220). Opened PR.

## Orientation (state at run start)
- `origin/main` @ `8e1c133` (#218). **Big change since the last run**: the PR queue
  drained from **7 → 2** (owner merged #181, #197, #199, #204, #205, #206, #212, plus
  the #213–#218 web series). Guardrail (≥3 open PRs) is **clear** — a new PR is allowed.
- **Open PRs: 2** — #219 (`fix(rec-engine)` #201, clean) and #221 (`feat(web)` sidebar).
- **Open issues: 11.** Top of the list: **#220 — "Trunk is red: #216 broke 4 tests"**
  (agent, prd:spending-intelligence). No open PR addressed it. Clear top priority:
  a red trunk on the core recommendation path, silent because CI is off (#72).

## What I did (Stage 4 — implement)
Reproduced the 4 failures on a clean `origin/main` (uv venv + `scripts/setup-test-db.sh`,
real Postgres): **4 failed, 280 passed** (284 collected — the issue's "293" was a stale
count; clean main collects 284 both before and after my edits).

**Root-caused all 4 to ONE bug — time-bomb fixtures, NOT the #216 classifier.**
`compute_profile` reads the **stored** `Transaction.category` (it does not re-classify),
so #216's rule changes cannot affect these tests. The real cause: fixtures seed
transactions at absolute **Jan–Apr 2026** dates, while the profile service computes a
**6-month lookback from `date.today()`**. On **2026-08-01** the cutoff advanced to
`2026-02-01`, aging every **January** seed txn out of the window:
- `test_spending_profile_service.py` (2 tests): the Jan-20 `travel` (Delta) txn dropped
  out → `assert "travel" in categories` failed; counts shifted.
- `test_recommendation_snapshot_service.py` (2 tests): the only seeded txn (Jan-5) aged
  out → empty profile → **empty `recommendations`** → `[0]` `IndexError`; empty
  `category_assignments` → `"shopping"` assertion failed.
The #216-merged-same-day coincidence is why the issue mis-attributed it.

**Fix (test-only; no production code touched):**
- `tests/conftest.py`: added `month_first_before_today(months_ago)` — first day of the
  calendar month N months before today, always inside the 6-month lookback.
- `test_spending_profile_service.py`: the 2 failing tests now pin
  `today=date(2026, 2, 28)` (the service accepts `today=`; matches the sibling
  window-slide tests, which already pin it — and the shared `_seed_transactions` dates
  are asserted verbatim by `test_recomputes_when_window_slides`, so pinning is correct
  rather than editing the helper).
- `test_recommendation_snapshot_service.py` + `test_recommendations_api.py`: the snapshot
  service has **no injectable clock** (`get_recommendations` calls `get_or_refresh` with
  the real `date.today()`), so their fixtures now anchor seed dates to the current month
  via the helper.
- **Bonus hardening:** `test_spending_profile_frequency_metrics` (currently green) was a
  latent time-bomb — its Mar/Apr-2026 dining txns would have aged out ~**Oct 2026** and
  turned trunk red again. Re-anchored to the two most recent whole months.

**Verification:** full suite **284 passed, 0 failed**; `ruff check tests/ app/` clean.

## PR
- Branch `agent/220-red-trunk-timebomb-fixtures` → **PR #<opened this run>**, `Closes #220`.

## QUESTIONS FOR HUMAN
1. **Enable GitHub Actions (#72).** This is the **second** time an hourly run has had to
   discover a red trunk by hand. With CI off, any merge can silently red the trunk and
   stay that way. One small workflow (`uv` + `scripts/setup-test-db.sh` + `pytest`) would
   catch this whole class at merge time. Highest-leverage ask.
2. The `travel → groceries` hypothesis in #220 is **not** a real misclassification — it
   was the window sliding. No classifier change is needed; the fix is entirely in the
   fixtures. (Recorded here so the follow-up the issue offered isn't pursued in error.)

_Generated by [Claude Code](https://claude.ai/code)_

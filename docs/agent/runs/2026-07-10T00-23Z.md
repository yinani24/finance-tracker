# Run journal — 2026-07-10T00-23Z

## Orientation (state at start)
- `origin/main` @ `a8ae711`. **Open agent PRs: 0** (queue clear). **Open agent issues: 1** — #38 (category-rate data source, BLOCKED on a human decision; unanswered across several runs).
- Verified the seed-priority pipeline is otherwise advanced: Plaid Phase 1 (#5–#8) and Phase 2 (#11, #23) are all **closed**; rec-engine slices 2 (#35), fee-waiver (#41) and alt-exclusions (#44) are **merged**. The rec engine is fully wired to API routes (`recommendations.py`), a snapshot cache, insights, and chat.
- No CI configured on the repo, so tests are owner-local; the sandbox can't `pip install -e ".[dev]"` (plaid-python wheel fails) → SQLAlchemy/pytest unavailable for DB-backed tests.

## Decision
- #38 is the only open issue and is blocked on the owner — I must **not** start category-aware code (slices 3–5 all gate on it). Per the prior run's guidance, look for genuine *unblocked* work instead of a filler PR.
- Read the rec-engine service + snapshot layer looking for a real, shippable correctness gap in the spirit of the earlier unblocked fixes #41/#44.

## What I advanced — Stage 1 (file) + Stage 4 (implement)
**Found and fixed a real cache-staleness bug.** `RecommendationSnapshotService.get_recommendations` keyed its `inputs_hash` on only the user's **spending profile** + **owned cards**, omitting the card **dataset** the recommendations are ranked over and `settings.points_value_cents`. So the GET read path (`/recommendations/next-card`, `/portfolio`) served a stale ranking after an upstream sign-up-bonus change / new card / discontinuation until the user changed their profile/cards or POSTed `/refresh` — directly contrary to PRD FR5 (Freshness) and the product's "best card by first-year value" promise. Fully unblocked, independent of #38.

- Filed **#47** (`agent`, `prd:recommendation-engine`) with verified evidence + acceptance criteria.
- Implemented on `agent/47-rec-cache-dataset-key` → **PR #48** (`Closes #47`):
  - Fetch the dataset **before** the cache check (process-wide TTL cache → in-memory read, no extra upstream round-trip; reused on miss so no double fetch).
  - `_compute_inputs_hash` made variadic; key = `sha256(profile | owned_cards | dataset | points_value_cents)`.
  - Recommendation outputs unchanged — only *when* the cache recomputes changes.
- Tests: new `test_dataset_change_recomputes` (changed bonus between two GETs → recompute, $200→$500); re-expressed `test_caches_on_second_call` to spy on `snapshot_repo.upsert` (cache hit must not recompute) since the cheap TTL fetch now runs each call; `test_invalidate_forces_recompute` still valid.
- Verified what the sandbox allows: `ruff` clean, module compiles, hash-invalidation logic checked in isolation (dataset change → new hash, point-value change → new hash, identical → stable). DB-backed tests are owner-local (see PR **VERIFY LOCALLY**).

## State after run
- Open agent PRs: **1** (#48). Open agent issues: **#38** (blocked), **#47** (→ #48).
- Under the 3-PR guardrail.

## QUESTIONS FOR HUMAN (carried — not blocking this PR; recorded on their issues)
1. **[BLOCKING rec-engine slices 3–5 + the dining-first premise]** #38 — category-rate data source. Pick: **(A)** in-repo curated category-rate table for the top ~20–30 cards + flat fallback [recommended], **(B)** adopt an alternative structured dataset, or **(C)** defer category-awareness and ship flat-cashback recs as v1.
2. **[carried, several runs]** #22 (manual-import epic) is CLOSED but only slice 1 shipped — reopen to track slices 2–4, or intentionally dropped?

## Next run (suggested)
- If #38 still unanswered: don't start category code. Review/land #48; otherwise doc/research/planning only, or another unblocked correctness slice if one surfaces.
- If owner picks a path on #38 → Stage 3 plan, then implement the chosen data path.

## Links
- Filed: **#47** · PR: **#48** (`Closes #47`) · Blocked/awaiting owner: **#38**, **#22**.

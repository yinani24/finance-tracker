# Run 2026-07-09T15-16Z

## Stage advanced
**Stage 5 — Iterate/land.** Reviewed and landed the single highest-priority
in-flight PR after verifying it end-to-end against a real Postgres, then kept
the multi-slice backlog accurate.

## Orientation
- `origin/main` @ `229db00` at start. Open agent PRs: **1** → #33 (CSV statement
  import, slice 1), `mergeable_state: clean`, freshly opened by the prior run.
  Under the 3-PR guardrail → finish in-flight work over starting new.
- Open agent issues: **#22** (manual import epic) only — addressed by #33.
- Confirmed the #30 force-push alarm (10:10Z) was already resolved: `main` is the
  FastAPI/Next.js app again (`git log` shows the `apps/api` tree; #30 closed).
- No CI configured (0 checks on every PR); mergeability + local verification are
  the only gates.

## What I did
1. **Reviewed PR #33 in full** (835+/50−, 16 files):
   - **Refactor (highest regression risk):** dedupe fingerprint extracted to
     `app/services/dedupe.py`, enrichment-apply to
     `app/services/enrichment/apply.py`; `plaid_service` delegates via
     `_generate_dedupe_hash` / `_apply_enrichment` aliases (still referenced in
     `sync_transactions` lines 169/216). Extracted bodies are byte-equivalent to
     the originals. `test_enrichment.py` `patch()` targets correctly repointed to
     `app.services.enrichment.apply.get_provider`.
   - **New import path:** `POST /imports` verifies account ownership (404 on miss),
     `run_import` records `Import`/`ImportFile`, `parse_csv` tolerates header
     aliases / multiple date formats / `$`,`,`,parens amounts, skips-and-counts bad
     rows, raises `StatementParseError` (→ 400, no partial rows) on bad/empty files.
     Intra-file dupes caught via SQLAlchemy autoflush before insert. `file_storage`
     sanitizes filenames against `../` path traversal. Correct 201/400/404 codes.
2. **Verified end-to-end against real Postgres 16** (brought up locally as an
   unprivileged user; recreated the test DB as **UTF8** after an initial SQL_ASCII
   initdb caused spurious `\xa2` (¢) encode failures — a test-env artifact, not a
   code bug): **full suite 209 passed** (196 prior + 13 new). `ruff` clean on all
   12 changed files. Alembic **single head** `a1b2c3d4e5f6`, linear chain (no
   migration this slice — import count derived at read time).
3. **Merged PR #33 (squash)** → `main` @ `04707bb`. GitHub auto-closed #22 as
   "completed", but #22 is a **4-slice epic** — **reopened** it and commented the
   slice-1-done / slices-2–4-remaining status so the backlog reflects reality.
4. Wrote this journal.

## State after run
- Open agent PRs: **0** (after this journal PR merges). Open agent issues: **#22**
  (slices 2–4 remaining).
- `main` head advanced by the Plaid-free CSV ingest path (slice 1).

## Follow-ups for next run
- **#22 slice 2 — column-mapping + preview** is the natural next slice (also
  resolves the amount-sign question per-file). Needs a Stage-3 plan before build.
- **Phase 3–4 recommendation engine:** no open agent issues exist yet; a future
  Stage-1 run could decompose `docs/prd/recommendation-engine.md` into slices
  (per-category earn from `profile.category_breakdown`, best-held-card, multi-card
  portfolio).
- **Repo-wide lint debt (non-blocking):** `ruff check app/ tests/` reports ~54
  pre-existing errors (mostly `E501` in older tests) unrelated to any recent PR.
  Worth a dedicated cleanup slice, or relax the line-length rule in `ruff` config.

## QUESTIONS FOR HUMAN (carried, non-blocking)
Carried from #33/#22 (slice 1 shipped on these defaults — all cheaply reversible):
1. **Amount sign** — CSV `amount` read as negative = spend / positive = income;
   slice 2's preview will let you confirm/flip per file. OK as the default?
2. **Format priority** — CSV first, PDF/OFX deferred to slice 3. Still right, or is
   PDF the format you actually have to hand?
3. **File storage** — uploads on local disk (`FT_IMPORT_STORAGE_DIR`) for dev; real
   object storage (S3 / Supabase) deferred behind the same `file_storage` interface.
   OK given these are sensitive documents?

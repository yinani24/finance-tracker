# Run journal — 2026-07-11T00-18Z

## Orientation (state at start)
- `origin/main` @ `827998b`. **Open agent PRs: 1** — #73 (last run's docs-only journal, awaiting merge). **Open agent issues: 2**, both owner-gated: **#38** (category-rate data source, blocks rec-engine slices 3–5) and **#72** (add CI test job — gated on Q1: enable metered GitHub Actions). Stage 5 empty (#70 merged; no feature PR in flight). Well under the 3-PR guardrail.
- Product pipeline is genuinely owner-gated: #38 (data-source A/B/C), spending Open Q2/Q3 (trend / recurring modeling). The codebase is mature — many rec-engine correctness slices already shipped (#41, #44, #47, #51, #52, #55, …).

## Decision
- Nothing in-flight was advanceable (no open feature PR to iterate; #38/#72 blocked on owner). Rather than manufacture a marginal bug-fix (barrel is nearly empty) or file a third consecutive doc-only run, I looked for a **clearly-good, owner-independent, verifiable** slice and found one: the lint debt that **#72's Q2 explicitly called to track** ("file a separate 'clear ruff debt' slice, then add a lint gate").
- Verified ground truth: `ruff check .` = **65 errors** (47 E501, 9 F401, 9 I001). **12 are in hand-written `app/` production source**; the rest are in `tests/` (~42, style-only) and autogenerated `alembic/versions/` (~11, belongs behind a per-file-ignore). Cleared the production-source 12 as **slice 1** — a lint gate can now start green at the `app/` layer, independent of the tests/migrations debt and of the (owner-gated) Actions decision.

## What I advanced — Stage 1 (#74) + Stage 4 (PR #75)
**Filed #74** and shipped **PR #75** (`Closes #74`, ref #72) on `agent/74-ruff-app-source`:
- **Removed 3 dead imports (F401)**, each confirmed unreferenced anywhere in its file: `sqlalchemy.func` (`app/repositories/insight.py`), `typing.Optional` + the unused `Insight` model (`app/services/insight_dispatcher.py`).
- **Wrapped 9 over-length lines (E501)** in `api/card_bonuses.py`, `api/chat.py`, `models/insight.py`, `repositories/insight.py`, `services/card_insight_engine.py` — Query descriptions, dict literals, a `UniqueConstraint`, a `.where(...)`. Pure reformatting, no statement altered.
- **Lint-only, no behavior change.** Checked `impact_*_cents` are non-nullable `int` (`default=0`), so the nearby `old_impact > 0` in `insight_dispatcher` is safe — not a latent bug.

**Verified:** `ruff check app/` → *All checks passed!* (repo total 65 → 53, dropped exactly the 12 targeted); all 6 touched files `python -m py_compile` clean. Full 235-test suite left as an optional owner VERIFY LOCALLY step (needs Postgres via the `uv` recipe) — the diff touches no logic. `CHANGELOG.md` updated under `[Unreleased]`.

## State after run
- Open agent PRs: **#75** (this slice) + #73 (prior journal) + this journal = 3. Open agent issues: **#38**, **#72** (both owner-gated), **#74** (has PR #75). At the 3-PR guardrail boundary — next run should prefer Stage 5 (review/land) over new work.

## QUESTIONS FOR HUMAN (carried — recorded on their issues, not phone-notified to avoid hourly noise)
1. **[BLOCKING rec-engine slices 3–5 + dining-first premise]** #38 — category-rate data source: (A) in-repo curated table [recommended], (B) alt dataset, (C) defer.
2. **[spending Phase 2]** spending PRD Open Q2 (trend/recency weighting) & Q3 (recurring vs discretionary separation) — modeling decisions gating the next spending slices.
3. **[infra]** #72 — enable metered GitHub Actions on this private repo? + lint-gate approach (tests-only first vs diff-scoped). Slice-2 ruff debt (tests/ + alembic per-file-ignore) tracked under #74.

## Next run (suggested)
- Prefer landing/reviewing #75 (and #73) over opening new work — at the guardrail. If owner answers #38, resume category-aware earn. Otherwise Stage 0–3 doc/research only; do not open filler PRs.

## Links
- Filed: **#74** · PR: **#75** branch `agent/74-ruff-app-source` (Closes #74, ref #72) · Owner-gated: **#38**, **#72**, spending Q2/Q3 · Prior journal PR: **#73**.

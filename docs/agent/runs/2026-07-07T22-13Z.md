# Agent run — 2026-07-07T22-13Z

**Stage advanced:** Stage 2 (Research) for **issue #12** — Method Financial vs Plaid
Liabilities for existing-card awareness (Phase 3–4 data source).

## What I did
- Oriented: Phase 1 (Plaid) is effectively complete pending owner merges — #5/#6/#7
  merged; **#8 is in PR #16** (`clean`, awaiting review) wiring real
  `Plaid-Verification` JWT checks over the previously no-op webhook stub. PR #15
  (journal) also open & clean. No CI configured; owner merges from phone. Nothing
  actionable for me on the open PRs (no review comments, no CI failures).
- #11 (enrichment) already has full Stage 2 research posted as an issue comment; its next
  stages are **blocked on 3 owner decisions** (keys, hosted-data privacy, taxonomy) — not
  advanceable this run.
- Picked the highest-priority *advanceable* item: **#12 research** (no prior research).
  Web-researched Method Financial vs Plaid Liabilities (2026 sources) and grounded the
  plan in the codebase (`Card` model is manual-only; no liabilities code exists).

## Output
- `docs/agent/research/12.md` — comparison, recommendation (**Plaid Liabilities for MVP**,
  Method deferred to Phase 3+), `held_card` model + provider-interface plan, risks.
- Mirrored findings as an issue comment on #12.

## Guardrail
2 open agent PRs before this (#15, #16) → opening this research PR makes 3, within the
"do not open a 4th" limit. Docs-only, no app code.

## QUESTIONS FOR HUMAN (also on #12)
1. Confirm MVP source = Plaid Liabilities (defer Method)?
2. Method needs its own keys + separate onboarding + sales-led pricing — defer OK?
3. Does APR matter for v1 value math (rewards + sign-up bonus only if pay-in-full)?
4. OK to show a manual-confirm UX when card-product auto-match confidence is low?

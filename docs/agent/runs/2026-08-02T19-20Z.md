# Run 2026-08-02T19-20Z — Stage 4: implement #208 slice 2a (enrichment provenance)

## Orientation (state read this run)
- **`main` @ `382444b`.**
- **Open agent issues (5):** #229 (has PR #239), #211, #210, #208, #185.
- **Open PRs (2):** #239 `agent/229-web-lint-set-state-in-effect` — `mergeable_state: clean`,
  awaiting owner merge, nothing to advance; #238 `feat/auto-extract` — **owner's own branch**,
  not agent-owned, left untouched.
- **Guardrail lifted:** only **1 open `agent/*` PR** (#239), so the ≥3-open-PR ceiling that
  jammed the last several runs is gone → PR budget is free.

## Decision — which single step
Two items are implementation-ready now that PR budget is free:
- **#208 slice 2a** (Phase 2, spending intelligence): persist `category_confidence` +
  `enriched_at`. Its Stage-2 research (issue comment, 2026-08-02) calls it "provider-independent,
  UNBLOCKED now… pure upside… the piece to implement first once PR budget frees up." No product
  decision required.
- **#185** (Phase 3-4, rec engine slice 5): fully planned, dependency #181 merged.

Picked **#208 slice 2a**: seed-priority order puts Phase 2 (spending intelligence) above Phase 3-4
(rec engine), it's the smaller/cleaner unit, and it's explicitly the recommended first
implementation. Not filler — a planned, unblocked, high-priority slice.

## What I did — Stage 4, branch `agent/208-persist-enrichment-confidence` (PR `Closes #208`? no — see below)
- **`app/models/transaction.py`:** added `category_confidence FLOAT NULL` + `enriched_at DATETIME NULL`.
- **`alembic/versions/b1c2d3e4f5a6_…py`:** additive migration, `down_revision = 2e3d11d4afb9`
  (verified single head). Both columns nullable, no server_default — a pre-enrichment row is
  genuinely "unknown", and `enriched_at IS NULL` is exactly the marker a future backfill (slice 2b)
  selects on.
- **`app/services/enrichment/apply.py`:** stamps `category_confidence = result.confidence` and
  `enriched_at = now(utc)` **only when the provider assigns a category**. Fail-open / noop rows
  (category None) stay unstamped → remain backfill candidates. Single shared write path, so both
  ingest routes (Plaid sync + statement import) get it identically.
- **`app/schemas/transaction.py`:** `TransactionRead` surfaces both fields (additive; `from_attributes`
  picks them up) → lights up low-confidence surfacing in the recategorize UI (#109).
- **Tests (`tests/test_enrichment.py`):** extended `test_provider_overwrites_category_and_merchant`
  to assert `category_confidence == 0.99` + `enriched_at is not None`; new
  `test_fail_open_leaves_provenance_unstamped` asserts both stay NULL when the provider blows up.

## Verification
- `ruff check` on changed files → clean.
- Full suite via `uv` venv + `./scripts/setup-test-db.sh` throwaway Postgres per CLAUDE.md:
  **313 passed**.
- Migration: `alembic upgrade head` applies cleanly; `alembic heads` → single head `b1c2d3e4f5a6`;
  `downgrade -1` → `upgrade head` round-trips without error.

## Scope boundary (deliberately NOT this slice)
- **Slice 2b** (implement `LLMProvider`/`ntropy` + `enrich_existing` backfill) — gated on the owner's
  privacy/provider decision (#208 Q1/Q2). Untouched.
- **Slice 2c** (dining-precision measurement vs keyword baseline) — untouched.
- The recategorize UI itself (#109) — this only persists + surfaces the value it needs.

## QUESTIONS FOR HUMAN
None blocking this slice — it's provider-independent and needs no product decision. The real gate
for the rest of #208 remains the owner's answer to its Q1/Q2 (LLM-via-Anthropic vs Ntropy vs
on-infra-only), unchanged by this PR.

## Next steps
- Review + merge this PR → slice 2a shipped; #109 recategorize UI can then read
  `category_confidence`.
- Answer #208 Q1/Q2 to unblock slice 2b (provider). Independently, **#185** (rec-engine slice 5) is
  the top queued engineering item and is implementation-ready for a future run's PR budget.

_Generated by [Claude Code](https://claude.ai/code)_

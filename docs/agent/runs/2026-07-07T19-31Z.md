# Run — 2026-07-07T19:31Z

## Stages advanced
- **Stage 5 (Iterate) + merges** on the Phase 1 Plaid line, per owner direction.
- **Stage 2 (Research)** for issue #11 (transaction enrichment) — posted as an issue comment.

## What happened
Owner reviewed the three open `agent/*` PRs and directed: merge the two that were clean, fix #13 first.

- **Merged #10** (`agent/6-plaid-error-handling`) — Plaid `ApiException` → 409/503/502 mapping. Squash-merged to `main`.
- **Merged #14** (`agent/8-plaid-webhook-verify`) — Stage 2 research doc for #8 (docs only). Squash-merged to `main`.
- **Fixed + merged #13** (`agent/7-plaid-webhook`) — `POST /plaid/webhook`:
  - Applied the one owner-approved fix ("do 1 only"): the `async` handler was calling the **blocking** `sync_transactions` (sync DB + Plaid HTTP) directly on the event loop, which would stall every other request for the sync's duration. Wrapped the sync + insights-event work in a `_sync_and_notify()` closure and offloaded it via `fastapi.concurrency.run_in_threadpool`. The handler stays `async` because it needs `await request.body()` for the #8 signature check.
  - Rebased the branch onto the freshly-merged `main` (clean auto-merge in `plaid_service.py`: both #10's `_call` wrapper and #13's `webhook=` kwarg present).
  - Deliberately **not** changed (owner said "1 only"): sync-error→200 retry-storm guard, the no-op `verify_webhook` stub until #8, inline (non-backgrounded) sync.

## Research posted (issue #11)
Ntropy vs Spade for transaction enrichment. Recommendation: **Ntropy for the MVP** (2,000-txn free tier, batch-enriches our stored transactions, confidence score) behind a provider-swappable `enrichment` interface; Spade as fallback. Proposed interface + backfill plan + taxonomy mapper. 3 QUESTIONS FOR HUMAN raised (Ntropy signup, hosted-provider data privacy, internal category set).

## Verification (real Postgres 16)
- `pytest tests/test_plaid_webhook.py tests/test_plaid.py` → **35 passed**.
- Full API suite → **163 passed**.
- `ruff check` on all branch-changed files → clean. (Pre-existing ruff errors elsewhere in `tests/` are untouched and out of scope.)

## QUESTIONS FOR HUMAN
- On #11: (1) OK to sign up for an Ntropy API key, or hold at interface-only? (2) both enrichment providers are hosted — send raw txn data off-infra, or investigate a self-hosted path first? (3) OK to define a fixed internal category set (dining first)?
- Owner-local (unchanged, PRD Q2): real end-to-end webhook verification needs a public `FT_PLAID_WEBHOOK_URL` + live Plaid keys.

## Next
- #8 (webhook JWT verification) is unblocked now that #7 (this PR) is merged — fills in `verify_webhook` + `FT_PLAID_WEBHOOK_VERIFY` on branch `agent/8-plaid-webhook-verify`.

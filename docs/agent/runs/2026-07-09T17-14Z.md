# Run journal — 2026-07-09T17-14Z

## Orientation
- Open PRs at start: **1** — #36 (previous run's journal, docs-only, `mergeable_state: clean`).
- Open `agent` issues at start: **1** — #35 (rec-engine ongoing-rewards slice, fully specced, no blocking questions).
- Under the 3-PR guardrail → free to advance in-flight work and start the one ready code slice.

## What I advanced
**Stage 5 (cleanup) + Stage 4 (implement).**

1. **Merged #36** (squash) — cleared the stale journal PR from the queue.
2. **Implemented #35** on branch `agent/35-ongoing-rewards` → PR (Closes #35).
   Added a first-year flat-cashback **ongoing-rewards term** to `recommend_next_card`.

### Change summary (`apps/api/app/services/card_recommendation.py`)
- New shared static helper `_ongoing_value(cashback_pct, avg_monthly_spend)` =
  `avg_monthly_spend × 12 × universalCashbackPercent / 100` (dollars).
- `recommend_next_card` score is now `bonus + ongoing − fee + credits`
  (was `bonus − fee + credits`, omitting ongoing entirely).
- Result dict gains an additive `ongoing_value` key; `explanation` now shows all
  four FR1 components (bonus / ongoing / credits / fee), e.g.
  `"... + ongoing $720 (2.0% on $36,000) ..."`.
- `analyze_portfolio` (both main calc and the alternatives loop) now delegates to
  the same `_ongoing_value` helper — single source of truth, no drift.

### Tests (`tests/test_card_recommendation_service.py`)
- New `TestOngoingRewards`: ongoing math, zero-cashback card, bonus+ongoing
  scoring, achievability still skips unachievable bonuses, explanation shows all
  four components, and the issue's **A-vs-B regression** (no-fee 2%-earn card at
  $3k/mo now correctly out-ranks a $95-fee bonus-heavy card: $870 vs $515).
- Updated the two hard-coded scores in `TestCrossTypeRanking` that legitimately
  shift with the new term (cashback still ranks first — intent preserved).
- 21/21 pass (`--noconftest`; conftest needs SQLAlchemy which won't install here —
  see below). `ruff` clean on the service; test file's only ruff hits are 3
  E501s pre-existing on `main`, left untouched.

## Environment note (not a code problem)
`pip install -e ".[dev]"` fails to build the `plaid-python` wheel in this sandbox
(`AttributeError: install_layout`, a setuptools incompatibility) — so the full
suite / conftest can't run here. My change is a pure function with no Plaid or DB
dependency, so it was verified in isolation with pytest `--noconftest`. Full-suite
CI on the owner's side is unaffected.

## QUESTIONS FOR HUMAN
Carried (non-blocking) from #35 / #36:
1. **Category-aware earn (next slice)** — this slice is flat `universalCashbackPercent`
   by design. Making earn depend on *how you spend* (dining first) needs per-category
   reward rates the card dataset doesn't yet expose. Confirm the order: ship flat now
   (this PR), then enrich `credit-card-bonuses-api` with category rates.
2. **#22 (manual-import epic) is CLOSED but only slice 1 shipped** — slices 2–4 are
   real remaining work. Reopen to track, or intentionally dropped?

## Links
- Issue: #35 · PR: this run's `agent/35-ongoing-rewards` · Merged: #36

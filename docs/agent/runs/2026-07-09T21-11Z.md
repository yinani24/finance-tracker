# Run journal — 2026-07-09T21-11Z

## Orientation (state at start)
- `origin/main` @ `8bbb47b`. **Open agent PRs: 1** — #42 (`fix(api): honor first-year fee
  waivers in recommend_next_card`, Closes #41), created by the prior run.
- **Open agent issues: 2** — #41 (fee-waiver bug, addressed by PR #42) and #38 (rec-engine
  slice 3, category-aware earn) which is **blocked on a human data-source decision** (carried).
- No CI configured on the repo → Stage 5 = own-review + merge.

## Decision — Stage 5: finish in-flight work before starting new
Under the PR guardrail (1 open PR, <3) and the "prefer finishing later-stage work" rule, the
highest-priority action was to review and land the already-open PR #42 rather than open anything
new. The only other backlog item (#38) remains human-blocked, so no new implementation was
warranted this run.

## What I did — reviewed & landed PR #42 (Closes #41)
- Pulled the PR diff and reviewed it for correctness (no CI, so self-review is the gate):
  - `_first_year_fee(card)` → `0.0` when `isAnnualFeeWaived` truthy, else `float(annualFee, 0)`. ✓
  - Score now subtracts `first_year_fee` instead of full `annual_fee`. ✓
  - Explanation waiver clause correctly guarded (`first_year_fee == 0 and annual_fee > 0`),
    otherwise `- fee $X`. Edge cases (fee 0 / waived-with-0-fee) fall to the plain branch. ✓
  - Score math verified against the tests: WAIVED = $200 bonus + $360 ongoing (1% of $36k)
    − $0 fee = **$560**; identical UNWAIVED twin = **$465** (560 − $95 fee) → waived out-ranks
    by exactly the fee. Regression: existing #35 scores (870/515) untouched. ✓
  - `analyze_portfolio` deliberately unchanged (keeps full recurring fee — steady-state keep/drop). ✓
- `mergeable_state: clean`, diff scoped and tight (167/−3 across 4 files). **Squash-merged** as
  `38685d8`. **#41 auto-closed** as completed.

## State after run
- Open agent PRs: **0** (queue clear). Open agent issues: **#38** (still blocked on human).
- Pipeline advanced by exactly one step (Stage 5 → landed).

## QUESTIONS FOR HUMAN (carried — the owner's decisions gate the next slices)
1. **[BLOCKING rec-engine slices 3–5 + the dining-first premise] Category-rate data source (#38).**
   The card dataset has **no per-category reward rates** (only flat `universalCashbackPercent`)
   and upstream is a read-only mirror that **won't accept PRs**. Pick a path:
   **(A) recommended** — small in-repo curated category-rate table for the top ~20–30 cards, flat
   fallback for the long tail; **(B)** adopt an alternative structured dataset (licensing to vet);
   **(C)** defer category-awareness and ship flat-cashback recs as v1. Until this is decided, the
   core "recommend based on how much you dine out" promise cannot ship. Detail in #38.
2. **[non-blocking, from #41/#42] First-year vs. steady-state in `analyze_portfolio`.** The
   fee-waiver fix applied only to `recommend_next_card` (apply-for-new). Should the held-card
   optimizer also surface a first-year (fee-waived) view alongside its steady-state keep/drop
   judgment, or is steady-state alone correct there?
3. **[carried] #22 (manual-import epic) is CLOSED but only slice 1 shipped** — slices 2–4
   (column-mapping, PDF/OFX, upload UI) are real remaining work. Reopen to track, or dropped?

## Links
- Landed this run: PR **#42** (merged `38685d8`), issue **#41** (closed, completed).
- Still blocked on human: **#38** (category-rate data source decision).

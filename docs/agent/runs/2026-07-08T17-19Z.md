# Run — 2026-07-08T17:19Z

## Stage advanced
- **Stage 4 (Implement) — issue #11, slice 1.** Built the provider-swappable
  transaction-enrichment layer with a keyless `noop` default and wired it into
  `sync_transactions`. Opened PR (branch `agent/11-enrichment-interface`, `Closes #11`
  is NOT used — this is slice 1 of a multi-slice issue, so the PR references #11 without
  closing it).

## Why this item
- The prior run (2026-07-07T19-13Z / triage 2026-07-08T14-11Z) was blocked by the 3-PR
  guardrail and explicitly queued "#11 slice 1" as the next action once the backlog
  cleared. The owner has since merged the backlog: `origin/main` is at `95bb3a6` and only
  **1** open `agent/*` PR remained (#19, the OAuth fix — fresh, no CI configured, no
  review comments → nothing to iterate). Guardrail clear (1 < 3).
- #11 already had Stage-2 research and a Stage-3 plan posted as issue comments; slice 1 is
  the **decision-independent** part (interface + noop), safe to build before the owner
  answers the data-privacy / API-key / taxonomy questions. So this run advances it to
  Stage 4.

## What changed
New `apps/api/app/services/enrichment/`:
- `base.py` — `EnrichmentInput` / `EnrichmentResult` Pydantic models + `EnrichmentProvider`
  Protocol. **Synchronous** interface (deviation from the plan's `async`) because the sole
  caller `sync_transactions` is a sync request path — documented in the module + research doc.
- `noop.py` — `NoopProvider`: echoes raw Plaid category + lowercased merchant → the hook is
  behavior-preserving until a real provider is configured.
- `taxonomy.py` — fixed internal category set + `map_to_internal` (Plaid PFC primaries +
  common vendor synonyms → internal; unknown/empty → `other`). Wired to real providers in
  slice 2; unit-tested now.
- `__init__.py` — `get_provider()` factory keyed off `FT_ENRICHMENT_PROVIDER`; unknown name
  falls back to `noop` with a warning (never breaks ingest).

Changed:
- `app/config.py` — `enrichment_provider: str = "noop"` (`FT_ENRICHMENT_PROVIDER`).
- `app/services/plaid_service.py` — collect newly-added rows, `_apply_enrichment` overwrites
  `category` / `normalized_merchant` before commit. **Fail-open**: provider error or
  mismatched batch → log + keep raw Plaid values.
- `.env.example`, root `CHANGELOG.md`.
- `docs/agent/research/11.md` — the deferred Stage-2 research doc now persisted.
- `tests/test_enrichment.py` — factory default/fallback, noop passthrough, taxonomy mapper,
  and sync-hook tests (overwrite fires / fail-open / default noop behavior-preserving).

## Verification (ran in-sandbox)
- Local Postgres 16 + clean venv (`pip install -e ".[dev]"` — needed a venv; the system's
  Debian-patched setuptools can't build `plaid-python`'s wheel, `install_layout` error).
- **`pytest` → 183 passed** (full suite; +9 new enrichment cases, all prior tests green).
- `ruff check` on all changed files → clean.

## Backlog state
- **#11** — slice 1 shipped in this PR (does not close #11; slice 2 = Ntropy provider +
  confidence/enriched_at migration + backfill, gated on owner answers).
- **#18 / PR #19** — OAuth redirect_uri fix, awaiting owner review (no CI configured on repo).
- **#12** — existing-card source research done (merged #17), blocked on owner answers.

## QUESTIONS FOR HUMAN (carried — block slice 2, not this PR)
1. **Data privacy** — Ntropy & Spade are hosted; enrichment would send raw merchant strings
   + amounts off-infra. OK for this personal app, or investigate self-hosted/on-device first?
2. **API key** — OK to sign up for Ntropy (2,000-txn free tier) for slice 2, or hold at `noop`?
3. **Taxonomy** — approve the fixed internal set (`dining, groceries, travel, transport,
   shopping, bills, entertainment, health, income, other`), dining first?
4. (From #18/#19, still open) which URL hosts Plaid Link per env for `FT_PLAID_REDIRECT_URI`;
   is live Chase (Production + OAuth) provisioned, or sandbox-only for now?

## Next run
- If owner answers Q1–Q3 → build **#11 slice 2** (real provider + migration + backfill).
- Else advance Stage 1: decompose Phase 2 spending-intelligence (dining habit metrics) or
  Phase 3–4 (existing-card ingestion + recommendation engine) into issues.

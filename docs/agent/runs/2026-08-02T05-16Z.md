# Agent run — 2026-08-02T05:16Z

**Stage:** Stage 2 (research) — researched **#209** (trajectory). Quiet run; one docs-only PR
(research/209.md + this journal). No code, no product decision guessed.

## Orientation (state at run start)
- `origin/main` @ `9ec017d` (#226). Owner has been **very active today**: queue drained 7→2
  and main advanced through #219–#226 (rec-engine currency/credit/PDF fixes, approval-odds
  ranking). No new merges in the last hour.
- **Open agent PRs: 2** (guardrail ≥3 → clear). Both `mergeable_state: clean`, no CI, no reviews:
  - **#227** `feat(web)` per-category "best held card" in Portfolio (Closes #183).
  - **#225** `fix(insights)` snooze date-only + effort badge (Closes #200).
  Both were opened by the owner's account today (03:16Z, 04:15Z) and await **owner review** —
  nothing mechanical to iterate (no red CI, no conflicts, no review threads).
- **Open agent issues: 7.** Forward backlog **#208–#211 is entirely owner-decision-gated** on
  Q1–Q7 in `docs/prd/data-architecture-and-evolution.md`:
  - #208 (classification provider) → Q2/Q3, already researched (`research/11.md`).
  - #209 (trajectory) → Q1, **un-researched** ← picked this run.
  - #210 (own MCP server spike) → Q4, un-researched.
  - #211 (held-card via Plaid Liabilities) → Q6, already researched (`research/12.md`).
  - #185 (rec-engine slice 5, multi-card set) depends on slice-4 PR #227 (unmerged).

## What I did (one step — Stage 2)
Wrote **`docs/agent/research/209.md`**: code-grounded research for the trajectory feature.
Key points captured there:
- `spending_profile.py` is a single-window snapshot (one row/user, `UniqueConstraint(user_id)`);
  the recommender already keys off `category_breakdown`, which is the exact zero-contract-change
  hook for a trend signal. No `transactions` schema change is needed for any of the three
  features.
- Proposed **bounded, recommendation-serving scope** (directly arms Q1): IN = category trend
  series + recurring-commitment/duplicate-sub detection + one explainable next-month projection;
  OUT = multi-horizon/ML forecasting, goal/net-worth projection.
- Architecture rec: **Option A — compute-on-read** (grouped monthly query, no migration/table),
  over Option B (persisted history table, deferred).
- 5-slice breakdown (trends → recurring → projection → recommender integration → frontend),
  each small + hermetic-testable.
- Flagged the recent **time-bomb-fixture** lesson (PR #220): trend code must take an injectable
  `today`/`as_of` and anchor test dates relative to it.
Also posted a condensed version as a comment on **#209** so the finding is visible on the issue.

## Notable correction — stale recurring ask about CI (#72)
Multiple recent journals (incl. 2026-08-02T02-18Z, 01-11Z, and the 22:11Z/23:12Z runs) list
**"Enable GitHub Actions (#72)"** as the top standing owner ask and describe "CI is off (#72)".
**#72 is CLOSED as completed** (by the owner, 2026-07-14) and `.github/workflows/` has never
existed in git history — its sole gate was owner Q1 ("enable metered Actions on this private
repo?"). The owner closing it as completed reads as a **resolved decision**, not an open ask.
**Future runs should stop re-flagging #72 as a live request.** If CI is genuinely still wanted,
it needs a *fresh* issue reopening the metered-Actions decision — not repetition against a
closed one. (I did not open a CI-workflow PR: authoring one would be guessing a closed infra
decision, against Absolute Rule #4.)

## Why not other options this run
- **In-flight PRs (#225/#227):** clean, owner-owned, awaiting owner review → no agent action.
- **Red web lint on `main`** (4× `react-hooks/set-state-in-effect` on legit SSR hydration
  guards + one page-reset effect): already escalated to the owner under QUESTIONS FOR HUMAN on
  *both* open PRs; the fix is partly a lint-policy call (suppress vs. refactor vs. disable rule)
  and, with CI off, isn't actively blocking merges. Not re-escalating; not fixing unilaterally.
- **#208/#211:** already researched; both owner-gated. **#185:** dep-blocked on #227.
- **#210:** un-researched but lower roadmap priority (Phase 4 tooling) than #209 (Phase 3,
  recommendation-serving). Next natural research target for a future quiet run.

## Owner notification — deliberately NOT sent
Owner is actively engaged this very window (opened #225/#227 at 03–04Z, merged 7+ PRs today).
The decision-gate on #208–#211 is not new (set up 19:19Z yesterday, documented in their own
data-arch doc), the two PRs are their own, and this run's value is captured in-repo (research +
comment). Pinging an actively-working owner at ~05Z with "please answer Q1–Q7" is exactly the
hourly noise to avoid. Stay silent until state changes.

## QUESTIONS FOR HUMAN (all standing — pipeline is fully decision-gated)
The forward backlog cannot advance to implementation until these are answered (all in
`docs/prd/data-architecture-and-evolution.md`):
1. **Q1 — Trajectory scope (#209).** Confirm the bounded, recommendation-serving scope in
   `research/209.md` §2 (trends + recurring detection + one explainable projection; **no**
   multi-horizon/ML forecasting), or hold Phase 3.
2. **Q2/Q3 — Classification provider + privacy (#208).** Ntropy vs Anthropic LLM vs stay
   rules-only; and whether raw merchant strings may leave infra.
3. **Q4 — MCP investment (#210).** Approve a time-boxed spike of our own read-first MCP server.
4. **Q6 — Held-card source (#211).** Confirm Plaid Liabilities for MVP, defer Method Financial.
5. **Review the 2 open PRs (#225, #227)** — both clean, frontend, provable from code.
6. **CI is a closed matter (#72).** Not re-asking — recorded above so it stops recurring.

_Generated by [Claude Code](https://claude.ai/code)_

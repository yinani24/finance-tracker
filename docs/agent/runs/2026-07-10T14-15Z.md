# Run journal — 2026-07-10T14-15Z

**Stage advanced:** Stage 0 (PRD accuracy) — reconciled `docs/prd/recommendation-engine.md`
with what the engine actually ships. Owner-independent; no filler PR.

## Orientation
- `origin/main` @ 6461193 → 99c11f7 after this run landed the orphaned journal PR #65.
- Open agent issues: **#38** only (rec-engine slice 3 — blocked on owner (A)/(B)/(C)
  category-rate data-source pick).
- Open PRs at start: **#65** only (13-20Z run journal, `clean`, doc-only).

## What I did
1. **Landed PR #65** (orphaned 13-20Z run journal, `mergeable_state: clean`, base = main
   HEAD). Squash-merged per repo convention to keep the tree at 0 open PRs before adding
   this run's work. Mirrors the 12-11Z run landing the orphaned #63.
2. **Found real doc drift the prior runs missed, and fixed it.** The 13-20Z run re-read
   `card_recommendation.py` for an owner-independent *code* fix and correctly found none
   (the code is correct). But it did not audit the **PRD** against that code. The
   rec-engine PRD predates two merged slices and still described them as unbuilt:
   - **Cents-per-point valuation (slice 1, shipped #28).** PRD Open Question 1 and the gap
     table claimed *"bonuses are summed as raw point counts with no ¢/point applied … must
     be fixed to rank fairly."* Verified false: `_bonus_value_usd` dollar-values bonuses
     (USD at face, points/miles at `points_value_cents`, default 1.0¢), threaded through
     `recommend_next_card`/`_best_achievable_offer`, covered by
     `test_card_recommendation_service.py` (`bonus_value == 1500.0` for 150k pts @ 1.0¢,
     etc.). Shipped in commit cc6f1a9 (#28).
   - **Flat ongoing-rewards term (slice 2, shipped #37).** Gap table row said *"Ongoing
     rewards (year 1): ❌ not computed — score is `bonus − fee + credits`."* Verified false:
     score is `bonus + ongoing − fee + credits` via the shared `_ongoing_value` helper
     (commit 0a099b2, #37).
   - **Edits:** Status line, FR1 (both bullets), the gap table (3 rows), the "bottom line",
     Open Question 1 (downgraded from blocking bug → resolved, live-and-flagged 1.0¢/point
     default), and the decomposition (slices 1–2 marked SHIPPED; 3–5 gated on #38).
     `CHANGELOG.md` under `[Unreleased] → Documentation`.

   This is the same class of correction as #61 (which fixed the spending-intelligence PRD's
   stale FR3 claim). It matters because a PRD that lists shipped work as "must-fix / not
   built" mis-signals both the owner and future runs about what's actually left.

## Verification
- No source/behavior change — doc-only. Grounded every claim in code + tests:
  `git log -- app/services/card_recommendation.py` (#28 cc6f1a9, #37 0a099b2) and
  `grep` of `tests/test_card_recommendation_service.py` (dollar-valued `bonus_value`
  assertions, `pts @` explanation test).
- Could not run `pytest` in-sandbox: `pip install -e ".[dev]"` can't build `plaid-python`
  here (legacy `setup.py` / setuptools `install_layout`) — sandbox tooling limitation,
  unchanged from prior runs, not a `main` regression and irrelevant to a doc-only change.

## Net pipeline state after this run
- Rec-engine slices 1–2: **shipped** (#28, #37). Slices 3–5: **blocked on #38**.
- Spending-intelligence: FR1–FR5 shipped (incl. #60 window-slide); trend/recurring slices
  gated on Open Q2/Q3.
- **No unblocked implementation work remains** until the owner decides #38 and the
  spending-intelligence Open Q2/Q3.

## QUESTIONS FOR HUMAN
No new questions. Two standing decisions gate all remaining feature work (unchanged):
1. **#38** — pick the category-rate data source ((A) in-repo curated table recommended).
   Blocks rec-engine slices 3–5 and the dining-first core premise. Research + a full
   Stage-3 plan for option (A) are already staged on the issue.
2. **Spending-intelligence Open Q2/Q3** (`docs/prd/spending-intelligence.md`) — frequency
   definition, trend weighting, recurring-vs-discretionary — needed before the trend /
   recurring-charge slices can be filed.

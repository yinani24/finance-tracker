# Run journal — 2026-07-15T20:12Z

## TL;DR
Quiet, **owner-gated** run at the code level — but **not** a re-verify-and-journal no-op.
The two open PRs (#169 feature, #170 last run's journal) both await owner review with clean
mergeable state, 0 reviews, no CI configured. Instead of re-checking #169 for the Nth time,
I advanced a genuine **Stage-2 verification** of the one standing non-gated TODO in the
codebase: the curated ongoing-earn rate table's `provenance` note ("pending owner
verification of individual rates before treating as ground truth").

**Result: all 14 curated `card_category_rates.json` multipliers verified accurate** against
published issuer reward schedules; mappings to internal categories are all defensible. One
substantive caveat surfaced (narrow-earn travel entries can overstate). Findings saved to
`docs/agent/research/card_category_rates_verification.md`.

## State verified this run (live GitHub + git)
- **Open PRs:** #169 (`agent/168-card-detail-page`, closes #168) — `mergeable_state: clean`,
  0 reviews, 0 CI checks (none configured); #170 (`agent/journal-2026-07-15T19-13Z`) —
  last run's journal, awaiting owner. Under the ≥3-PR feature guardrail; this run adds a
  **docs/research** PR (allowed — Stage 0–3 doc work), not a feature PR.
- **Open issues:** only #168 (closed-by #169).
- **Rec engine re-checked:** category-aware ongoing earn is **fully wired** —
  `_ongoing_value(card, avg, category_breakdown)` is called by both `recommend_next_card`
  (L262) and `analyze_portfolio` (L363/L410); `spending_profile` produces
  `category_breakdown`; the `/recommendations/*` API is consumed by the web
  (`getNextCardRecommendations`, `getPortfolioRecommendations`). No missing engine plumbing.
- Point/mile valuation (Open Q1) already resolved (1.0¢/point default, flagged, #28).

## Stage advanced
**Stage 2 — Research/verification** of `card_category_rates.json` (the #38 path-A table,
#165). Verified 14/14 base multipliers accurate; documented one overstatement caveat and
the unchanged out-of-scope items (caps/rotating/portal). Did **not** mutate the owner-owned
data table (owner asked to verify before ground-truth; the two open Qs are product/data
decisions, not guessed — Absolute Rule #4).

## Why not a feature PR
Everything code-wise is owner-gated (2 unreviewed PRs already queued). Stacking a 3rd
feature PR on top is the "produce faster than the owner reviews" anti-pattern. A documented
verification pass that discharges a real standing TODO is genuine forward progress and the
right quiet-run move.

## Links
- Research: `docs/agent/research/card_category_rates_verification.md`
- Data verified: `apps/api/app/data/card_category_rates.json` (+ its README)
- Engine: `apps/api/app/services/card_recommendation.py` (`_ongoing_value`, L132)

## QUESTIONS FOR HUMAN
1. **Bless the rate table?** All 14 rates verified accurate — OK to flip `provenance` from
   "pending owner verification" to verified-as-of-2026-07?
2. **Narrow-earn travel policy:** keep optimistic `Amex Platinum travel: 5` / `Amex Gold
   travel: 3` (whole-`travel`-category), or down-rate so travel-heavy users aren't
   over-promised? (Today these only ever inflate estimates.)
3. **Standing (unchanged):** review/merge #169 (card-detail UI, verified clean last run) and
   #170 (journal).

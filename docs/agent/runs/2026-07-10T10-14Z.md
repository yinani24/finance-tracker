# Run journal — 2026-07-10T10-14Z

**Stage:** 4 (Implement) — issue #60.

## Orientation
- `origin/main` @ b654fed. Open agent issues: **#60** (spending-intelligence FR5, unblocked/owner-independent) and **#38** (rec-engine slice 3, blocked on owner data-source decision — already flagged in QUESTIONS FOR HUMAN, no new info). **0 open PRs.**
- Guardrail (3+ open agent PRs) not hit → free to open one. Picked the single highest-priority *unblocked* item: **#60**.

## What I advanced
Implemented the FR5 window-slide freshness fix in `apps/api/app/services/spending_profile.py`:
- `get_or_refresh` now recomputes when the current 6-month lookback cutoff has advanced past the cached profile's earliest transaction (`period_start`) — i.e. a month with data has aged out — even when **no new transaction** arrived. Previously it only recomputed on a newer transaction, so averages (dining $/mo, `monthly_avg_count`) stayed inflated until the next ingest.
- Chose `current_cutoff > existing.period_start` as the trigger: migration-free (no schema change), hermetic, and fires exactly when the averages would change (a txn ages out). No-op when the window is unchanged, so the existing cache-hit path is preserved.
- Made `today` injectable on `_lookback_start`/`compute_profile`/`get_or_refresh` (default `date.today()`, additive) so the slide is testable without freezing the clock. All existing callers unchanged.
- Tests: added `test_recomputes_when_window_slides` and `test_caches_when_window_unchanged`; existing `test_caches_when_no_new_transactions` stays green. CHANGELOG updated under `[Unreleased]`.

## Verification
- Sandbox **cannot build `plaid-python`** (`install_layout` setuptools error) → DB-backed pytest is owner-local/CI (per issue #60 note). Put under `## VERIFY LOCALLY` in the PR.
- Validated the pure date-math + trigger logic standalone: slide scenario recomputes; same-window is a cache hit; the existing back-to-back cache test never trips the slide for any real run month (period_start is always ≥ cutoff right after a compute). `ruff check` on the diff clean (one pre-existing I001 on `main` in an untouched test method left alone).

## PR
- Branch `agent/60-spending-profile-window-slide` → PR (Closes #60).

## QUESTIONS FOR HUMAN
- None new. #38 remains blocked on the category-rate data-source decision (option A recommended) — owner input still needed there.

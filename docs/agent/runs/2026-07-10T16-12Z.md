# Run journal — 2026-07-10T16-12Z

**Stage advanced:** Stage 1 (backlog) — filed **#68**, a real owner-independent
tech-debt slice (dependency pinning / build reproducibility). No filler PR.

## Orientation
- `origin/main` @ `759fd92` (#67) — current; fetched fresh.
- **0 open PRs** — nothing in-flight (Stage 5 empty).
- Open `agent` issues at start: **1** — #38 (rec-engine slice 3, category-aware earn),
  fully gated on the owner's (A)/(B)/(C) card category-rate data-source pick. Its
  Stage-2 research + a complete Stage-3 plan for option (A) are already staged on the
  issue; it cannot reach Stage 4 without the decision. Nothing to add without repeating.
- Spending-intelligence remaining slices (trend, recurring-charge) still gated on
  owner Open Q2/Q3. FR1–FR5 shipped.

## Why not another pure-journal quiet run
The last three runs (13-20Z, 14-15Z, 15-11Z) correctly declined to manufacture filler
while the feature pipeline sits owner-gated. But those journals repeatedly flagged one
**genuine, owner-independent, untracked** risk — the unbounded `>=` dependency pins with
no lockfile — as a live problem, never captured in the authoritative backlog. Filing an
issue (not a PR) is exactly the blessed "quiet run that files issues is a good run" move,
and it converts a recurring journal footnote into tracked work.

## What I did
1. **Verified the risk against ground truth**, didn't assume it from prior journals:
   - `apps/api/pyproject.toml` — all 11 runtime deps + 4 dev deps use unbounded `>=`.
   - No lockfile anywhere (`uv.lock` / `poetry.lock` / `requirements*.txt` — searched,
     none).
   - `plaid-python>=29.0.0` resolves to **40.1.0** today (sdist-only, legacy `setup.py`
     → `AttributeError: install_layout` under the sandbox setuptools). This is the exact
     root cause of the "can't run pytest in-sandbox" limitation every recent run hits.
2. **Confirmed no existing issue tracks it** (searched issues; only keyword-matched the
   closed Plaid webhook issue #8, unrelated).
3. **Filed #68** — proposes upper-bound caps + a lockfile + identifying a build-clean
   `plaid-python` version, with two QUESTIONS FOR HUMAN (lock tooling choice → `uv`
   recommended; exact `plaid-python` cap, which needs a build-capable env the sandbox
   isn't). Labeled `agent`. Framed as hygiene/tech-debt, not a PRD slice.

Why this is worth doing beyond hygiene: capping `plaid-python` to a PEP-517-buildable
version would likely restore full in-sandbox `pytest`, raising verification confidence on
every future run — a direct payoff to the agent pipeline itself.

## Verification
- No source/behavior change this run (issue + journal only). All claims in #68 grounded
  in `pyproject.toml` + a filesystem lockfile search performed this run.
- `pytest` still not runnable in-sandbox for the reason #68 documents — unchanged tooling
  limitation, not a `main` regression.

## Net pipeline state
- **Plaid Phase 1** — done.
- **Spending intelligence (Phase 2)** — FR1–FR5 shipped; trend/recurring gated on Open Q2/Q3.
- **Recommendation engine** — slices 1–2 shipped (#28, #37); slices 3–5 blocked on #38.
- **New:** #68 (build reproducibility) — owner-independent; actionable once a build-capable
  env confirms the `plaid-python` cap + lock tooling is chosen.

## QUESTIONS FOR HUMAN
No new *product* questions. Standing decisions (unchanged; not re-notified to avoid hourly
noise — already recorded where you'll see them):
1. **#38** — pick the category-rate data source ((A) in-repo curated table recommended).
   Blocks rec-engine slices 3–5 and the dining-first core premise. Research + plan staged.
2. **Spending-intelligence Open Q2/Q3** (`docs/prd/spending-intelligence.md`) — trend
   weighting and recurring-vs-discretionary modeling — before those slices can be filed.
3. **#68** (new) — lock tooling preference (`uv` recommended) + the `plaid-python` cap
   target (needs a build-capable env). Low-urgency but unblocks in-sandbox testing.

## Links
- Filed: #68 · Standing gate: #38 · `origin/main` @ 759fd92 (#67)

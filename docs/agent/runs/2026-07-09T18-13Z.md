# Run journal — 2026-07-09T18-13Z

## Orientation
- `origin/main` @ `91e918b`. Open agent PRs: **1** (#37, rec-engine ongoing-rewards
  slice 2, `mergeable_state: clean`). Open agent issues: **1** (#35, closed by #37).
- Under the 3-PR guardrail.

## Stage advanced
**Stage 2 — Research** (no code slice; the only in-flight work, #37, is clean).

### Why not iterate on #37
Reviewed PR #37's full diff. The change is correct and complete: `_ongoing_value`
shared helper (`avg_monthly_spend × 12 × pct/100`), score `bonus + ongoing − fee +
credits`, additive `ongoing_value` key, A-vs-B regression ($870 vs $515, A first),
`TestCrossTypeRanking` rescored to 540/980 (cashback still first), and
`analyze_portfolio` delegates to the same helper. No bug, no CI configured in the
repo (`.github/workflows` absent → no checks to babysit), no review comments. It
awaits owner review — not agent-mergeable (product code, not a docs journal). So no
Stage-5 work exists this run.

### What I did — resolved the recurring category-rate blocker to ground truth
Every remaining rec-engine slice (3 category-aware earn, 4 optimize-wallet, 5
multi-card) and the product's dining-first premise are gated on the same open
question (PRD Open Question 3): the card dataset has no per-category reward rates.
Prior runs carried this as a vague QUESTION FOR HUMAN. This run pinned it down:

1. **Consumer side exists** — `spending_profile.py` builds `category_breakdown`
   over the 10-category taxonomy (`enrichment/taxonomy.py`). We know the user's
   dining/grocery/travel share already.
2. **Card side is empty** — `card_recommendation.py:82` names `category_breakdown`
   as input but never reads it. `credit-card-bonuses-api/exports/data.json`
   (179 cards) has **no** per-category earn field — only flat
   `universalCashbackPercent` (1% → 137, 2% → 25, 1.5% → 15, 5% → 2). `details` is
   free-text on just 8/179 cards, not machine-readable.
3. **Upstream can't be enriched** — the sibling repo is a read-only mirror of a
   private monorepo and "Pull requests will not be accepted here" (MIT + Commons
   Clause). → PRD Open Question 3 option (a) "enrich the sibling API" is **not
   available**. This is new information vs. what prior runs assumed.

Wrote `docs/agent/research/38.md` (full findings + options), and **filed issue #38**
(`agent`, `prd:recommendation-engine`) framing the data-source decision with a
recommended default: **(A) in-repo curated category-rate table for the top ~20–30
cards, flat `universalCashbackPercent` fallback for the long tail** — the smallest
slice that makes the dining-first promise real without depending on an
un-contributable upstream. Fallbacks: (C) defer + ship flat-cashback v1; (B) adopt
an alternative dataset.

## State after run
- Open agent PRs: **1** (#37) + this docs PR. Open agent issues: **#35** (→ #37),
  **#38** (slice 3, blocked on the owner's data-source pick).
- Slice 3+ stay blocked until #38's QUESTIONS FOR HUMAN is answered.

## Follow-ups for next run
- If owner picks (A): Stage 3 plan on #38, then implement the curated table + the
  category-aware term in `recommend_next_card` (extend `_ongoing_value`).
- #37 still awaits owner review/merge.
- #22 slices 2–4 remain (issue CLOSED — carried below).

## QUESTIONS FOR HUMAN
1. **Category-rate data source (blocks slices 3–5 + the dining-first premise).**
   The card dataset has no per-category earn rates and the upstream mirror won't
   accept PRs. Pick: **(A) maintain a small in-repo curated category-rate table**
   (recommended), (B) adopt an alternative structured dataset, or (C) defer
   category-awareness and ship flat-cashback recs (#37) as v1. Detail in #38.
2. **#22 (manual-import epic) is CLOSED but only slice 1 shipped** — slices 2–4
   (column-mapping/amount-sign, PDF/OFX, upload frontend) are real remaining work.
   Reopen to track, or intentionally dropped? (Carried, unresolved several runs.)

## Links
- Research: `docs/agent/research/38.md` · Issue filed: #38 · Reviewed (no change): #37

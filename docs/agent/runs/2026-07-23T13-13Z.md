# Run 2026-07-23T13-13Z

## Orientation
- Open agent PRs (2, under the 3-PR guardrail):
  - **#181** rec-engine slice 4 (`category_assignments`) — `mergeable_state: clean`, based on current main HEAD, no reviews, no CI (Actions still disabled, #72). Awaiting owner review. Nothing agent-actionable.
  - **#197** next-card currency fix (#196) — `mergeable_state: clean`, opened prior run. Awaiting owner review.
- Blocked issues: **#183** (slice-4 frontend) and **#185** (slice-5 combination) both depend on #181 merging. **#196** already covered by PR #197. **#177** implemented → PR #181.

## What I advanced
**Stage 4 — implemented #198** (Transactions category filter dead-end). Independent, frontend-only, touches none of the in-flight rec-engine files.

- Branch `agent/198-transactions-filter-taxonomy` off latest `origin/main`.
- `apps/web/src/app/(app)/transactions/page.tsx`: the category-filter `<select>` derived its options from the returned rows, but `filterCategory` is applied **server-side** (in the query key + request), so selecting a category collapsed the dropdown to "All Categories" + the active category — a dead-end forcing a reset-to-All before switching. Options now come from the canonical `TAXONOMY_CATEGORIES` constant already in the file. Also dropped a now-unnecessary non-null assertion and added `capitalize` to match the row-level category select.
- `CHANGELOG.md`: entry under `[Unreleased] → Fixed`.

## Verification
- `npm install` + `npx eslint` on the changed file: **no new problems**. The one remaining eslint error (`react-hooks/set-state-in-effect` at the `setPage(1)` effect, line ~96) is **pre-existing on `origin/main`** (from #195) — confirmed by linting the stashed pristine version. Not introduced here; out of scope for this fix.
- No backend change; no live Plaid/Postgres needed to verify (pure rendering logic).

→ PR opened to close #198.

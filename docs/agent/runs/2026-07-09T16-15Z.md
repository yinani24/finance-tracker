# Run 2026-07-09T16-15Z

## Stage advanced
**Stage 1 — Issues.** Decomposed the ready recommendation-engine PRD one step
further: filed the next shippable slice (#35) as a GitHub issue. No PR/merge risk;
backlog was empty, so this advances the pipeline without opening filler code.

## Orientation
- `origin/main` @ `b6ed060`. Open agent PRs: **0**. Open agent issues: **0**.
- Backlog is empty — every prior slice has landed: Plaid Phase 1 (#5–#8), enrichment
  (#11), frequency metrics (#23/#25), CSV import slice 1 (#22/#33), bonus points→$
  valuation (#24). No in-flight Stage-5 work to finish first.
- Two carried threads (see below): #22 is CLOSED but its epic has slices 2–4 open;
  the rec-engine PRD lists 5 decomposition slices, of which #24 shipped slice 1.

## What I did
1. Read PRDs + recent journals; confirmed 0 open PRs / 0 open issues via GitHub.
2. **Verified the rec-engine gap in code** (`app/services/card_recommendation.py`):
   `recommend_next_card` scores `bonus_val - annual_fee + credit_val` — **ongoing
   rewards omitted** — while `analyze_portfolio` already models ongoing earn via the
   dataset's flat `universalCashbackPercent`. The two modes are inconsistent, and
   `next_card` contradicts the owner-confirmed Decision #1 (total first-year value
   includes ongoing rewards). This is the PRD's flagged "missing half."
3. **Filed issue #35** — "add first-year ongoing-rewards term to `recommend_next_card`
   (flat cashback)": rec-engine PRD decomposition **slice 2**, unblocked (flat
   `universalCashbackPercent`, no dataset change, no owner answers required per PRD
   Open Question 3 recommendation (b)). Includes a concrete A-vs-B ranking bug, scoped
   non-goals (category-awareness deferred to slice 3), and acceptance criteria.
   Labels: `agent`, `prd:recommendation-engine`.
4. Wrote this journal.

## State after run
- Open agent PRs: **0** (until this journal PR merges). Open agent issues: **#35**
  (rec-engine ongoing-rewards, ready for Stage-2/3 research+plan, then Stage-4 build).

## Follow-ups for next run
- **#35** is the highest-priority next item — Phase 4 is the actual "recommend a card"
  payoff. Next step: Stage-3 plan (extract the shared flat-earn helper) then implement.
- **#22 slices 2–4** (column-mapping/preview, PDF/OFX, frontend) remain but the issue
  is **CLOSED** — the prior run's reopen did not stick (see QUESTIONS FOR HUMAN).
- **Rec-engine slices 3–5** (category-aware earn, optimize-wallet best-held-card,
  multi-card portfolio) can be filed as issues in later Stage-1 runs; slice 3 is
  gated on per-category reward rates (PRD Open Question 3).

## QUESTIONS FOR HUMAN
1. **#22 (manual import epic) is CLOSED but only slice 1 shipped.** Slices 2–4
   (column-mapping + amount-sign confirmation, PDF/OFX, upload frontend) are real
   remaining work. Reopen #22 to track them, or were they intentionally dropped? I did
   not re-fight the closed state this run.
2. **Rec-engine category-awareness ordering** (carried on #35): ship flat-cashback
   ongoing rewards now, then enrich `credit-card-bonuses-api` with per-category rates
   for true "based on how you dine out" ranking? PRD recommends this order.

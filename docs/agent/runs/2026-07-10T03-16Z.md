# Run journal — 2026-07-10T03-16Z

## Orientation (state at start)
- `origin/main` @ `044570c`. **Open agent PRs: 0** (queue clear). **Open agent issues: 1** — #38 (category-rate data source, BLOCKED on an owner A/B/C decision; Stage-2 research + Stage-3 plan already posted across prior runs — nothing left to advance there without the owner).
- Seed pipeline otherwise advanced: Plaid Phase 1 (#5–#8) + Phase 2 (#11, #23) closed; rec-engine slices #35/#41/#44/#47 merged.
- No CI on the repo; sandbox can't `pip install -e ".[dev]"` (plaid-python wheel build fails) → DB-backed pytest is owner-local. `ruff` is available.

## Decision
- #38 stays blocked on the owner — do NOT start category-aware code, and do NOT re-post the plan (that would be churn; two prior runs already did research + plan). Instead hunted for a genuinely **unblocked** correctness gap, in the spirit of #47.
- Found a real one and shipped it (Stage 1 file + Stage 4 implement, one small PR). Well under the 3-PR guardrail (queue was empty).

## What I advanced — Stage 1 (file #51, #52) + Stage 4 (PR for #51)
**Found: the internal taxonomy is never applied at ingest.** `taxonomy.map_to_internal` — documented in `base.py` as THE vendor-agnostic boundary and unit-tested by `TestTaxonomyMapper` — is **never called in the app** (grep hits only its definition + a docstring). Trace:
- `plaid_service.py:183` stores `category = personal_finance_category.primary` lowercased → raw `"food and drink"`.
- The default (only) `NoopProvider` **echoes that raw label back** instead of mapping it.
- `spending_profile.py:81` keys `category_breakdown` on it → the profile is keyed on **raw Plaid strings, not the internal taxonomy**.
- Impact: #38's pre-staged plan keys on `category_breakdown["dining"]`, which would **never match** `"food and drink"` — silently zeroing the dining signal the whole product optimizes on. Fixture drift already visible (some tests seed `"dining"`, others `"food and drink"`).

**Filed:**
- **#51** — apply the taxonomy at the boundary (this PR).
- **#52** — split groceries from dining via Plaid's `personal_finance_category.detailed` (depends on #51; `primary` can't distinguish them, so `groceries` stays an empty bucket until then). Filed, not implemented — kept this slice tight.

**Implemented #51** on `agent/51-taxonomy-at-ingest` → PR (`Closes #51`):
- `enrichment/noop.py`: `category = map_to_internal(plaid_category)` when present, else `None`; `raw_provider_category` keeps the raw label. The `None`-stays-`None` guard preserves the statement-import no-overwrite path (`map_to_internal(None)` would return `"other"` and clobber statement-derived categories).
- Tests: reframed the two noop tests to assert internal-taxonomy output (`FOOD_AND_DRINK` → `"dining"`); added a `None`-passthrough test; the provider-failure fail-open test (keeps raw) stays valid unchanged.
- `CHANGELOG.md` under `[Unreleased]`.
- Verified in-sandbox: `ruff` clean, modules compile, isolated mapping-logic check passes (raw→internal, `None`→`None`, unknown→`other`). DB-backed pytest is owner-local (VERIFY LOCALLY on the PR).

## State after run
- Open agent PRs: **1** (#51). Open agent issues: **#38** (blocked on owner), **#52** (follow-up, unstarted). Under the 3-PR guardrail.

## QUESTIONS FOR HUMAN (carried — recorded on their issues)
1. **[BLOCKING rec-engine slices 3–5 + the dining-first premise]** #38 — category-rate data source: **(A)** in-repo curated category-rate table [recommended; plan pre-staged], **(B)** alternative structured dataset, or **(C)** defer. Sole blocker on all remaining category-aware work.
2. **[carried]** #22 (manual-import epic) is CLOSED but only slice 1 shipped — reopen to track slices 2–4, or intentionally dropped?

## Next run (suggested)
- Review/land #51; then #52 (groceries/`detailed` split) is the next unblocked slice, independent of #38.
- If #38 still unanswered: hold on category-aware code; #52 gives real, unblocked progress on category accuracy meanwhile.

## Links
- Filed: **#51**, **#52** · PR: **#51** branch `agent/51-taxonomy-at-ingest` · Blocked/awaiting owner: **#38**, **#22**.

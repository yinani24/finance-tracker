# Run journal — 2026-07-10T09-11Z

## Orientation (state at start)
- `origin/main` @ `999112c`, then fetched `7c0ce98` after this run's first action.
- Open agent PRs: **1** — #59 (Phase-2 Spending-Intelligence PRD, docs-only, opened ~an
  hour ago, `mergeable_state: clean`). Open agent issues: **1** — #38 (card-side
  category-rate data source, BLOCKED on owner A/B/C). #22 (manual-import epic) closed,
  also awaiting owner.
- Under the 3-PR guardrail. No CI; sandbox can't build `plaid-python`, so DB pytest is
  owner-local.

## What I advanced — Stage 5 (land in-flight) + Stage 1 (file next slice)

### 1. Reviewed & merged PR #59 (Stage 5)
Verified the PRD's code claims against source before landing (the real risk in a docs PR
is misrepresenting code):
- FR3 gap-as-drafted, FR2 shipped, FR5 freshness edge, taxonomy — all checked against
  `spending_profile.py` / the `SpendingProfile` model. Baseline docs accurate; FR5 edge
  real. Squash-merged → `7c0ce98`.

### 2. Found the PRD's headline "gap" was already shipped — corrected it
While scoping the PRD's recommended next slice (FR3, "primary unblocked slice"), I read
`app/api/recommendations.py` (which the original PRD audit had **not** covered) and found
FR3 is **already delivered and tested**:
- `GET /recommendations/spending-profile` computes `monthly_avg_count = cnt / months`
  (`recommendations.py:57`) and surfaces a first-class `dining` rollup
  (`recommendations.py:62,70`).
- Tested by `tests/test_recommendations_api.py::test_spending_profile_frequency_metrics`
  (asserts `monthly_avg_count == 2.0`, `avg_per_txn == 50.0`, `data["dining"] == dining_cat`).

So filing FR3 as "the primary unblocked slice" would have been redundant work. Applied a
surgical correction to `docs/prd/spending-intelligence.md` (FR3 section, gap table, Net
paragraph, decomposition) + a `CHANGELOG` note, marking FR3 shipped and promoting FR5 to
the next slice. **This is the value of the run: it stops a future run from rebuilding a
shipped feature.**

### 3. Filed the genuinely-unshipped slice — #60 (Stage 1)
**#60 — FR5 freshness edge:** `get_or_refresh` recomputes only when a newer transaction
exists (`spending_profile.py:137`); when wall-clock time crosses into a new month with no
new txn, the 6-month lookback window slides but the cached profile (and its averages) go
stale until the next ingest. Bounded/self-healing but real, and it silently skews the
recommendation inputs. No test covers the window-slide (`test_caches_when_no_new_transactions`
only covers the cache-hit path). The issue includes acceptance criteria (injectable
"today" for hermetic testing, additive-only, preserve cache-hit) and a prominent
"FR3 is already shipped — do NOT rebuild it" note. Labels: `agent`, `prd:spending-intelligence`.
Unblocked — independent of #38 and #22.

## State after run
- Open agent PRs: 1 (this journal + PRD-correction PR). Open agent issues: #38 (blocked),
  #60 (new, unblocked, ready for Stage 2/3/4).
- No product decision guessed.

## QUESTIONS FOR HUMAN (carried — unchanged, still the only blockers)
1. **[BLOCKING rec-engine slices 3–5] #38** — pick the category-rate card-side data source:
   (A) in-repo curated table [recommended], (B) third-party dataset, or (C) defer
   category-awareness. Unanswered across many runs.
2. **#22** — manual-import epic is closed with only slice 1 shipped. Reopen for slices 2–4,
   or done for now?
3. **[non-blocking, from the Phase-2 PRD]** dining "frequency" = per-transaction (current,
   shipped) vs distinct-days? trend/recency weighting wanted? separate recurring vs
   discretionary? Safe defaults already ship; these only scope future refinements.

## Next run (suggested)
- If #38 or #22 lands → Stage 4 on the chosen path.
- Else → advance **#60** (FR5 freshness edge): it's the smallest unblocked, owner-independent
  slice — go Stage 2/3 (research/plan) or straight to Stage 4 (implement). Genuinely-unshipped,
  needs no owner decision.

## Links
- Merged: PR #59 (`7c0ce98`). Filed: #60. Corrected: `docs/prd/spending-intelligence.md`
  (FR3 status). Still blocked on owner: #38, #22.
